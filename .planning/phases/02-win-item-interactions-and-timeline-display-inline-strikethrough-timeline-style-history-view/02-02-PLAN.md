---
phase: 02-win-item-interactions-and-timeline-display-inline-strikethrough-timeline-style-history-view
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/history/DayDetail.jsx
autonomous: true
requirements: [TIMELINE-01, TIMELINE-02]

must_haves:
  truths:
    - "History day detail renders as vertical dot-and-line timeline"
    - "Completed wins show filled dot and foreground left-border accent"
    - "Incomplete wins show hollow dot and muted border"
    - "No timestamps are displayed anywhere in the timeline"
    - "Incomplete win notes still expand/collapse on click"
  artifacts:
    - path: "src/components/history/DayDetail.jsx"
      provides: "Timeline layout with dots, connecting line, colored accents"
      contains: "border-l"
  key_links:
    - from: "src/components/history/DayDetail.jsx"
      to: "wins[].check_ins[0].completed"
      via: "check_ins join data from useHistory"
      pattern: "check_ins.*completed"
---

<objective>
Redesign DayDetail from a flat bordered list into a vertical dot-and-line timeline. Each win is a timeline item with a dot on a vertical line, colored left-border accent, and completion-aware styling. No timestamps displayed per user direction.

Purpose: Visual hierarchy matching task management app reference — dots and connecting lines create a timeline feel for the history view.
Output: Rebuilt DayDetail.jsx with TimelineItem sub-component replacing WinRow.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-win-item-interactions-and-timeline-display-inline-strikethrough-timeline-style-history-view/02-RESEARCH.md

<interfaces>
<!-- Current DayDetail interface -->

From src/components/history/DayDetail.jsx:
```javascript
// WinRow({ win }) — reads win.check_ins?.[0]?.completed and win.check_ins?.[0]?.note
// DayDetail({ date, wins = [], loading = false }) — renders date header + list of WinRow
// wins array items have: { id, title, check_ins: [{ completed, note }] }
```

From src/hooks/useHistory.js:
```javascript
// fetchWinsForDate returns wins with check_ins join:
// select('*, check_ins(*)') — each win has check_ins array
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace WinRow with TimelineItem in DayDetail</name>
  <files>src/components/history/DayDetail.jsx</files>
  <action>
Replace the `WinRow` component with a `TimelineItem` component and update the DayDetail container layout. Keep both components in the same file (co-located, matching current pattern).

**TimelineItem component:**
- Props: `{ win, isLast }` (isLast suppresses the connecting line segment after the final item)
- Read `completed` from `win.check_ins?.[0]?.completed`
- Read `note` from `win.check_ins?.[0]?.note`
- Layout: `relative pl-8 pb-6` (last item: `pb-0`)
- **Dot**: absolute positioned circle on the vertical line
  - `absolute left-0 top-1.5 w-3 h-3 rounded-full border-2`
  - Completed: `bg-foreground border-foreground` (filled)
  - Incomplete: `bg-background border-border` (hollow)
  - Center on line: `style={{ transform: 'translateX(-50%)' }}` with `left: 7px` (half of ml-[7px] on container)
- **Card content** (right of line):
  - Left border accent: `border-l-2 pl-4 py-1`
  - Completed: `border-foreground`
  - Incomplete: `border-border/40`
  - Title: `font-mono text-sm block`
  - Completed title: add `line-through text-muted-foreground`
  - Incomplete title: `text-foreground`
  - Status badge: retain existing `Completed`/`Incomplete` mono badge styling
- **Note expand/collapse**: preserve the existing AnimatePresence expand pattern from WinRow for incomplete wins with notes. Keep ChevronDown/ChevronUp toggle.

**DayDetail container update:**
- Wrap the wins.map in: `<div className="ml-[7px]">` — the vertical line is NOT a border-l on this container. Instead, each TimelineItem except the last draws a connecting segment.
- OR simpler: use `border-l border-border ml-1.5` on the wrapper div, with dots positioned to overlap the line. Remove border-l from the last item's wrapper using isLast.
- Choose whichever approach looks cleaner during implementation. The key constraint: dots must visually sit ON the vertical line, not beside it.

**Do NOT:**
- Add any timestamps or time display
- Use a third-party timeline library
- Change the DayDetail props interface (date, wins, loading)
- Remove the loading or empty state renders

**Test existing DayDetail tests still pass.** The tests check for loading state, date rendering, and win title rendering. The new layout must preserve these behaviors. If tests assert on specific class names from WinRow, update test assertions to match TimelineItem markup.
  </action>
  <verify>
    <automated>mise exec -- bun run test --run</automated>
  </verify>
  <done>DayDetail renders vertical timeline with dots on a connecting line. Completed wins: filled dot + foreground border accent + line-through title. Incomplete wins: hollow dot + muted border + normal title. No timestamps anywhere. Note expand/collapse preserved. All tests pass.</done>
</task>

</tasks>

<verification>
- `mise exec -- bun run test --run` — all tests green
- DayDetail renders timeline layout (border-l connecting line, absolute dots)
- Completed wins have filled dots and foreground accents
- Incomplete wins have hollow dots and muted accents
- No time/timestamp text rendered
</verification>

<success_criteria>
- Vertical dot-and-line timeline replaces flat list in DayDetail
- Dots visually sit on the connecting line (centered, not offset)
- Completed/incomplete states clearly distinguished (filled vs hollow, foreground vs muted)
- Line-through on completed win titles in timeline view
- No timestamps displayed
- Note expand/collapse still works for incomplete wins
- All existing tests pass (updated if needed for new markup)
</success_criteria>

<output>
After completion, create `.planning/phases/02-win-item-interactions-and-timeline-display-inline-strikethrough-timeline-style-history-view/02-02-SUMMARY.md`
</output>
