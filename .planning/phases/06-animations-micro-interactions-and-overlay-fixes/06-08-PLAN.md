---
phase: 06-animations-micro-interactions-and-overlay-fixes
plan: 08
type: execute
wave: 3
depends_on:
  - 06-02
  - 06-03
  - 06-04
  - 06-05
  - 06-06
  - 06-07
files_modified: []
autonomous: false
requirements:
  - FIX-01
  - FIX-02
  - FIX-03
  - FIX-04
  - FIX-05

must_haves:
  truths:
    - "All five original bugs are resolved — verified in browser"
    - "Every interactive button across the app responds visibly to press"
    - "Journal save crossfades to summary screen"
    - "StreakCelebration animates correctly in and out"
    - "HistoryPage date changes crossfade smoothly"
  artifacts: []
  key_links: []
---

<objective>
Visual acceptance checkpoint — verify all Phase 6 fixes and micro-interactions in browser before phase closes.

Purpose: Active: pseudo-class behavior, CSS animation timing, and visual crossfades are not meaningfully testable in jsdom. Human verification of the complete set of changes is the phase gate.
Output: Approved or issue list to address.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-animations-micro-interactions-and-overlay-fixes/06-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and start dev server</name>
  <files></files>
  <action>
    Run full test suite:
    ```
    mise exec -- npm run test -- --run
    ```
    All tests must be green before presenting the checkpoint.

    Then start the dev server:
    ```
    mise exec -- npm run dev
    ```
    Confirm it starts on the expected port (currently 5177).
  </action>
  <verify>
    <automated>mise exec -- npm run test -- --run</automated>
  </verify>
  <done>Full test suite green. Dev server running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual acceptance — verify all Phase 6 fixes in browser</name>
  <files></files>
  <action>
    Present the visual acceptance checklist to the user and await "approved" response.
  </action>
  <verify>
    <automated>MANUAL — active: pseudo-class and CSS animation timing not testable in jsdom</automated>
  </verify>
  <done>User types "approved" after verifying all checklist items in browser.</done>
  <what-built>
    Phase 6 complete — all original bugs fixed and full app press-feedback sweep applied:

    Original bugs:
    - FIX-01: TimerFocusOverlay Add slot visible with 4+ wins
    - FIX-02: TodayPage no longer flashes when timer overlay opens
    - FIX-03: Journal save crossfades to summary (no hard cut)
    - FIX-04: Journal dismiss is snappier (0.22s ease-in vs 0.35s spring)
    - FIX-05: Journal Save button shows "Saving..." while saving

    New fixes:
    - StreakCelebration: animates in/out correctly (was silently broken)
    - All buttons across app: have active: press feedback
    - HistoryPage: DayDetail crossfades on date change
    - DayDetail: note expand/collapse animates height (no snap)
  </what-built>
  <how-to-verify>
    Open http://localhost:5177

    **FIX-01 — Timer overlay 4-win cap:**
    1. Log 4 or more wins on Today page
    2. Click the timer icon on any win to open TimerFocusOverlay
    3. Confirm the "+" Add slot is still visible

    **FIX-02 — TodayPage flash:**
    1. On Today page with at least one win, click the timer icon
    2. Confirm the win list does NOT flicker or go blank during the overlay open

    **FIX-03 — Journal save crossfade:**
    1. Open Journal, click New Entry
    2. Type a title and body
    3. Click Save
    4. Confirm the editor fades out (moving up) and the summary screen fades in from below — no hard cut

    **FIX-04 — Journal exit easing:**
    1. Open a journal entry overlay
    2. Click the X to dismiss
    3. Confirm the dismiss feels faster/snappier than the entry animation

    **FIX-05 — Save button saving state:**
    1. Open journal, type a title, click Save
    2. If the save is fast: confirm button briefly showed "Saving..." (may need a slow network to observe)
    3. Save button should be disabled while saving (no double-submit)

    **StreakCelebration:**
    1. If you can trigger a streak increment: verify the celebration portal animates in and out
    2. Or inspect src/components/layout/StreakCelebration.jsx to confirm `exiting` state and `overlay-enter`/`overlay-exit` classes are present

    **Press feedback sweep — spot check these:**
    - Today page: "Log a win" button → press and release → observe brief scale-down
    - Today page: WinCard timer play/pause/stop buttons → press → observe brief opacity dim
    - RollForwardPrompt (if visible): Yes button → hover shows opacity change
    - CheckInOverlay: start a check-in → Yes/No buttons → observe scale press
    - MorningPrompt (if visible): buttons have press response
    - SideNav: click nav items → observe subtle bg flash
    - History page: click day cells → observe scale press
    - History page: click different days → DayDetail crossfades
    - History page: click expand on an incomplete win with a note → observe animated height
    - Journal page: "New Entry" button → press → observe opacity dim
    - Journal page: hover entry → Edit/Delete buttons → press → observe opacity dim

    Type "approved" if everything looks correct, or describe what needs fixing.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 6, or describe issues found.</resume-signal>
</task>

</tasks>

<verification>
Human visual sign-off required. All automated tests must be green before checkpoint is presented.
</verification>

<success_criteria>
- Full test suite green
- All FIX-01 through FIX-05 verified in browser
- StreakCelebration animation confirmed working
- Representative sample of press-feedback buttons tested
- Human types "approved"
</success_criteria>

<output>
After approval, create `.planning/phases/06-animations-micro-interactions-and-overlay-fixes/06-08-SUMMARY.md`
</output>
