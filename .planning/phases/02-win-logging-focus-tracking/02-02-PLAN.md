---
phase: 02-win-logging-focus-tracking
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/hooks/useStopwatch.js
  - src/hooks/useWins.js
  - src/stores/uiStore.js
autonomous: true
requirements: [TIMER-01, TIMER-02, WIN-01, WIN-02, WIN-03, WIN-04]

must_haves:
  truths:
    - "useStopwatch returns correct displaySeconds using wall-clock math (not setInterval accumulation)"
    - "useStopwatch correctly recovers elapsed time after simulated page refresh"
    - "useWins exposes addWin, editWin, deleteWin, rollForward, startTimer, pauseTimer actions"
    - "uiStore manages inputOverlayOpen and editingWinId without React Context"
  artifacts:
    - path: "src/hooks/useStopwatch.js"
      provides: "useStopwatch(elapsedBase, startedAt) hook + formatElapsed(seconds) util"
      exports: ["useStopwatch", "formatElapsed"]
    - path: "src/hooks/useWins.js"
      provides: "Primary data hook: fetch today's wins, CRUD actions, timer actions, roll-forward"
      exports: ["useWins"]
    - path: "src/stores/uiStore.js"
      provides: "Zustand store for overlay and editing state"
      exports: ["useUIStore"]
  key_links:
    - from: "src/hooks/useWins.js"
      to: "src/lib/supabase.js"
      via: "import supabase"
      pattern: "import.*supabase.*from.*@/lib/supabase"
    - from: "src/hooks/useWins.js"
      to: "src/lib/utils/date.js"
      via: "import getLocalDateString"
      pattern: "import.*getLocalDateString"
    - from: "src/stores/uiStore.js"
      to: "zustand"
      via: "import { create } from 'zustand'"
      pattern: "from.*zustand"
---

<objective>
Build the data and logic layer: useStopwatch hook, useWins data hook, and Zustand UI store.

Purpose: Components in Wave 3 import these contracts. By building the logic layer first, component tasks receive exact function signatures and types — no exploration needed.
Output: Three files that components consume. useStopwatch tests turn green. No UI yet.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-win-logging-focus-tracking/02-RESEARCH.md
@.planning/phases/02-win-logging-focus-tracking/02-01-SUMMARY.md
@src/lib/supabase.js
@src/lib/utils/date.js
@src/hooks/useStopwatch.test.js

<interfaces>
<!-- Supabase wins table schema (from Phase 1 migration) -->
Wins table columns:
  id: uuid (PK)
  user_id: uuid (matches VITE_USER_ID)
  title: text
  win_date: text (YYYY-MM-DD, set by client via getLocalDateString())
  status: text default 'pending'
  timer_elapsed_seconds: integer default 0
  timer_started_at: timestamptz nullable
  created_at: timestamptz

<!-- Env vars available -->
import.meta.env.VITE_USER_ID   — user UUID for all DB operations
import.meta.env.VITE_SUPABASE_URL
import.meta.env.VITE_SUPABASE_ANON_KEY
import.meta.env.VITE_USER_JWT

<!-- Existing utilities -->
import { supabase } from '@/lib/supabase';
import { getLocalDateString } from '@/lib/utils/date';
// getLocalDateString() → 'YYYY-MM-DD' (today in local timezone)
// getLocalDateString(new Date(Date.now() - 86400000)) → yesterday
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement useStopwatch hook</name>
  <files>src/hooks/useStopwatch.js</files>
  <behavior>
    - useStopwatch(null, null): isRunning=false, displaySeconds=0
    - useStopwatch(30, null): isRunning=false, displaySeconds=30
    - useStopwatch(0, isoString): isRunning=true, displaySeconds = floor((Date.now() - new Date(isoString)) / 1000)
    - useStopwatch(30, isoString): isRunning=true, displaySeconds = 30 + floor((Date.now() - new Date(isoString)) / 1000)
    - Refresh recovery: same elapsedBase + same startedAt but called 10s later → displaySeconds includes those 10 extra seconds
    - formatElapsed(0) → "00:00"
    - formatElapsed(65) → "01:05"
    - formatElapsed(3661) → "01:01:01"
  </behavior>
  <action>
    Implement `src/hooks/useStopwatch.js` with `useStopwatch(elapsedBase, startedAt)` and `formatElapsed(totalSeconds)`.

    Wall-clock pattern (CRITICAL — do NOT use setInterval accumulation):
    - `liveElapsed = startedAt ? Math.floor((Date.now() - new Date(startedAt).getTime()) / 1000) : 0`
    - `displaySeconds = elapsedBase + liveElapsed`
    - setInterval(1000) only triggers re-renders via a `tick` counter in state — it does NOT accumulate elapsed time
    - The interval is only registered when isRunning=true; clean up on unmount/isRunning change

    Export both `useStopwatch` and `formatElapsed` as named exports.

    Run tests after implementation: `npx vitest run src/hooks/useStopwatch.test.js -x`
    All useStopwatch tests must be green before committing.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useStopwatch.test.js --reporter=verbose</automated>
  </verify>
  <done>All useStopwatch tests pass. formatElapsed formats HH:MM:SS or MM:SS correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement useWins hook and uiStore</name>
  <files>src/hooks/useWins.js, src/stores/uiStore.js</files>
  <action>
    Create `src/stores/uiStore.js`:
    ```js
    import { create } from 'zustand';

    export const useUIStore = create((set) => ({
      inputOverlayOpen: false,
      editingWinId: null,
      rollForwardOffered: false,

      openInputOverlay: () => set({ inputOverlayOpen: true }),
      closeInputOverlay: () => set({ inputOverlayOpen: false }),
      setEditingWin: (id) => set({ editingWinId: id }),
      clearEditingWin: () => set({ editingWinId: null }),
      markRollForwardOffered: () => set({ rollForwardOffered: true }),
    }));
    ```

    Create `src/hooks/useWins.js` with the following exported hook:
    ```js
    export function useWins() {
      // returns: { wins, loading, error, yesterdayWins, addWin, editWin, deleteWin, rollForward, startTimer, pauseTimer, stopTimer }
    }
    ```

    Implementation details for useWins:
    - `wins` state: array of win objects from Supabase (today's wins, ordered by created_at ASC)
    - Fetch on mount: `.select('*').eq('win_date', getLocalDateString()).eq('user_id', VITE_USER_ID).order('created_at', { ascending: true })`
    - `yesterdayWins`: array of yesterday's wins. Fetch on mount: `.select('title, id').eq('win_date', yesterday).eq('user_id', VITE_USER_ID)`. Used to show/hide roll-forward prompt.
    - `addWin(title)`: insert `{ user_id, title, win_date: today, timer_elapsed_seconds: 0, timer_started_at: null }`, append to wins state optimistically (then replace with DB response)
    - `editWin(id, newTitle)`: update `{ title: newTitle }` for that id, update local wins state
    - `deleteWin(id)`: delete by id, remove from local wins state
    - `rollForward()`: bulk insert copies of yesterdayWins with today's win_date and zeroed timer fields; append to wins state
    - `startTimer(winId)`: update `{ timer_started_at: new Date().toISOString() }`, update local wins state
    - `pauseTimer(winId, displaySeconds)`: update `{ timer_elapsed_seconds: displaySeconds, timer_started_at: null }`, update local wins state (snapshot elapsed BEFORE clearing startedAt)
    - `stopTimer(winId, displaySeconds)`: identical to pauseTimer for Phase 2

    Always include `.eq('user_id', import.meta.env.VITE_USER_ID)` on every Supabase mutation (defense-in-depth alongside RLS).
    Do NOT call supabase.auth.* methods — user identity comes from import.meta.env.VITE_USER_ID only.

    No tests exist for useWins at this wave — it is tested implicitly through component tests in Waves 3-4.
  </action>
  <verify>
    <automated>npx vitest run src/hooks/ --reporter=verbose</automated>
  </verify>
  <done>useStopwatch tests still green. useWins and uiStore files exist with correct exports. No TypeScript/ESLint errors from npm run build.</done>
</task>

</tasks>

<verification>
npx vitest run src/hooks/ --reporter=verbose — all useStopwatch tests green.
npm run build — no module resolution errors.
</verification>

<success_criteria>
- src/hooks/useStopwatch.js: all tests green, wall-clock pattern only (no setInterval accumulation)
- src/hooks/useWins.js: exported useWins hook with 8 actions
- src/stores/uiStore.js: Zustand create() store with inputOverlayOpen, editingWinId, rollForwardOffered
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-win-logging-focus-tracking/02-02-SUMMARY.md`
</output>
