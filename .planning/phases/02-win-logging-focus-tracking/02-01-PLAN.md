---
phase: 02-win-logging-focus-tracking
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/hooks/useStopwatch.test.js
  - src/components/wins/WinInputOverlay.test.jsx
  - src/components/wins/WinCard.test.jsx
  - src/components/wins/RollForwardPrompt.test.jsx
  - src/components/wins/TotalFocusTime.test.jsx
  - package.json
  - package-lock.json
autonomous: true
requirements: [WIN-01, WIN-02, WIN-03, WIN-04, TIMER-01, TIMER-02, TIMER-03]

must_haves:
  truths:
    - "motion and zustand are installed and importable"
    - "Five test stub files exist with failing tests that describe each requirement"
    - "All subsequent waves have failing tests waiting to be made green"
  artifacts:
    - path: "src/hooks/useStopwatch.test.js"
      provides: "Failing stubs for TIMER-01 and TIMER-02 (wall-clock arithmetic, isRunning, refresh recovery)"
    - path: "src/components/wins/WinInputOverlay.test.jsx"
      provides: "Failing stubs for WIN-01 (render, submit, Escape)"
    - path: "src/components/wins/WinCard.test.jsx"
      provides: "Failing stubs for WIN-02 (inline edit) and WIN-03 (delete)"
    - path: "src/components/wins/RollForwardPrompt.test.jsx"
      provides: "Failing stubs for WIN-04 (prompt count display, confirm/dismiss callbacks)"
    - path: "src/components/wins/TotalFocusTime.test.jsx"
      provides: "Failing stubs for TIMER-03 (sum with live running timer, returns null at 0)"
  key_links:
    - from: "src/hooks/useStopwatch.test.js"
      to: "src/hooks/useStopwatch.js"
      via: "import — will be created in Wave 2"
      pattern: "import.*useStopwatch"
    - from: "src/components/wins/WinInputOverlay.test.jsx"
      to: "src/components/wins/WinInputOverlay.jsx"
      via: "import — will be created in Wave 2"
      pattern: "import.*WinInputOverlay"
---

<objective>
Install motion and zustand, then create failing test stubs for all Phase 2 behaviors.

Purpose: Nyquist compliance — every implementation wave has automated verification waiting. No Wave 2+ task ships without a pre-existing failing test turning green.
Output: Two new packages in package.json, five test stub files (all failing at start, all passing at phase end).
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-win-logging-focus-tracking/02-CONTEXT.md
@.planning/phases/02-win-logging-focus-tracking/02-VALIDATION.md
@src/test-setup.js
@src/hooks/useTheme.test.js
@src/lib/utils/date.test.js

<interfaces>
<!-- Existing test infrastructure from Phase 1 -->

From vitest.config.js (already configured):
- environment: jsdom
- setupFiles: ['src/test-setup.js']
- resolve.alias: { '@': '/path/to/src' }

From src/test-setup.js:
- window.matchMedia stub already present
- @testing-library/jest-dom extended

From src/lib/supabase.js:
```js
import { createClient } from '@supabase/supabase-js';
export const supabase = createClient(url, key, { accessToken: async () => USER_JWT });
```

From src/lib/utils/date.js:
```js
export function getLocalDateString(date = new Date()) // returns 'YYYY-MM-DD' via Intl en-CA
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install motion and zustand</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run: npm install motion zustand

    Verify the install produced motion@^12.x and zustand@^5.x in package.json dependencies.
    Do NOT add --save-dev — both are runtime dependencies used in production components.
    Do NOT install framer-motion — the package is named `motion` (rebranded).
  </action>
  <verify>
    <automated>node -e "require('./node_modules/motion/react'); require('./node_modules/zustand'); console.log('ok')"</automated>
  </verify>
  <done>package.json has motion and zustand in dependencies; node_modules contain both</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test stub files for all Phase 2 behaviors</name>
  <files>
    src/hooks/useStopwatch.test.js,
    src/components/wins/WinInputOverlay.test.jsx,
    src/components/wins/WinCard.test.jsx,
    src/components/wins/RollForwardPrompt.test.jsx,
    src/components/wins/TotalFocusTime.test.jsx
  </files>
  <behavior>
    useStopwatch.test.js (TIMER-01, TIMER-02):
    - isRunning is false when startedAt is null
    - isRunning is true when startedAt is an ISO string
    - displaySeconds equals elapsedBase when not running (startedAt null)
    - displaySeconds equals elapsedBase + floor((Date.now() - startedAt) / 1000) when running
    - displaySeconds is correct after simulated page refresh: same startedAt, same elapsedBase, computed at a later Date.now()

    WinInputOverlay.test.jsx (WIN-01):
    - Renders the full-screen overlay when open=true
    - Does not render when open=false
    - Submitting the form with a non-empty title calls onSubmit with that title trimmed
    - Pressing Escape calls onClose
    - Submitting with empty/whitespace title does NOT call onSubmit

    WinCard.test.jsx (WIN-02, WIN-03):
    - Renders the win title
    - Clicking the edit trigger (e.g., pencil icon or title) shows an editable input pre-filled with the title
    - Submitting the inline edit form (Enter) calls onEdit with the new title
    - Pressing Escape in inline edit mode cancels and restores display
    - Clicking the delete button calls onDelete

    RollForwardPrompt.test.jsx (WIN-04):
    - Renders correctly with count=1 (singular: "1 unfinished win from yesterday")
    - Renders correctly with count=3 (plural: "3 unfinished wins from yesterday")
    - Clicking "Yes" calls onConfirm
    - Clicking "Dismiss" calls onDismiss

    TotalFocusTime.test.jsx (TIMER-03):
    - Returns null (renders nothing) when totalSeconds across all wins is 0
    - Shows correct sum when wins have timer_elapsed_seconds and no running timers
    - Includes live delta from timer_started_at in the total when a timer is running
  </behavior>
  <action>
    Create all five test files. Use `vi.fn()` for callbacks. Use `vi.setSystemTime` + `vi.useFakeTimers` in useStopwatch tests to control Date.now(). Use `@testing-library/react` render + userEvent in component tests. Mock `@/lib/supabase` at module level in component tests via `vi.mock('@/lib/supabase')`.

    Each test file imports from the not-yet-created source file — ALL tests must fail with "module not found" or equivalent error. That is the expected Wave 0 outcome.

    CRITICAL: Do NOT create the source files (useStopwatch.js, WinInputOverlay.jsx, etc.) — those are Wave 2 work.

    File structure for stubs:
    ```js
    // src/hooks/useStopwatch.test.js
    import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';
    import { renderHook } from '@testing-library/react';
    import { useStopwatch } from './useStopwatch';
    // ... tests
    ```

    ```jsx
    // src/components/wins/WinInputOverlay.test.jsx
    import { describe, it, expect, vi } from 'vitest';
    import { render, screen } from '@testing-library/react';
    import userEvent from '@testing-library/user-event';
    import WinInputOverlay from './WinInputOverlay';
    // ... tests
    ```

    TotalFocusTime test renders with a `wins` prop (array of win objects with `timer_elapsed_seconds` integer and `timer_started_at` string or null).
  </action>
  <verify>
    <automated>npx vitest run src/hooks/useStopwatch.test.js src/components/wins/ --reporter=verbose 2>&1 | grep -E "FAIL|Cannot find module|failed"</automated>
  </verify>
  <done>Five test files exist, all fail with module-not-found errors (source files do not exist yet). Running the test suite shows 5 failing test suites, 0 passing. This is the correct Wave 0 state.</done>
</task>

</tasks>

<verification>
npm run build passes (no TypeScript errors from test files).
npx vitest run src/hooks/useStopwatch.test.js src/components/wins/ shows all 5 test suites failing with import errors — confirms stubs are in place and ready for implementation.
</verification>

<success_criteria>
- motion and zustand installed (runtime deps)
- 5 test stub files exist with meaningful test descriptions covering all 7 requirements
- All 5 test suites fail (import errors) — correct Wave 0 state
- No source implementation files created
</success_criteria>

<output>
After completion, create `.planning/phases/02-win-logging-focus-tracking/02-01-SUMMARY.md`
</output>
