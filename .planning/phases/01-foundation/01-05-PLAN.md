---
phase: 01-foundation
plan: "05"
type: execute
wave: 2
depends_on:
  - "01-02"
  - "01-03"
  - "01-04"
files_modified: []
autonomous: false
requirements:
  - SHELL-01
  - SHELL-02

must_haves:
  truths:
    - "App runs in browser showing the Today empty state with today's date in Geist Mono"
    - "Dark/light toggle works — clicking the sun/moon button switches modes instantly without flash"
    - "Dark mode preference persists — after toggling dark and hard-refreshing, app opens in dark mode"
    - "Bottom tab bar is visible with three tabs; active tab is visually distinct from inactive tabs"
    - "Dot grid background is visible (very subtle texture) in both light and dark modes"
    - "Font renders as Geist Mono (confirmed in DevTools Computed styles)"
  artifacts: []
  key_links: []
---

<objective>
Human visual acceptance of the complete Phase 1 foundation — routing, design system, and dark mode toggle working together in the browser.

Purpose: Automated tests verify hook behavior and build output, but cannot verify visual quality, font rendering, dot grid opacity, or the absence of theme flash. This checkpoint gates Phase 2 — feature work must not begin on a broken or visually wrong foundation.
Output: User confirmation that Phase 1 foundation looks and behaves correctly.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and dev server</name>
  <files></files>
  <action>
    Run the full Vitest suite to confirm all automated tests pass:
    ```
    npx vitest run --reporter=verbose
    ```

    Then start the dev server:
    ```
    npm run dev
    ```

    Report the dev server URL and all test results to the user before the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/justin/Repositories/Personal/wintrack && /Users/justin/.local/share/mise/installs/node/22.22.0/bin/node node_modules/.bin/vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>All automated tests pass; dev server is running</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual acceptance of Phase 1 foundation</name>
  <files></files>
  <action>Human visual verification — see how-to-verify instructions below.</action>
  <verify><automated>MISSING — visual/interactive verification; human must confirm in browser</automated></verify>
  <done>User approves Phase 1 visual design and interactive behavior</done>
  <what-built>
    Plans 01–04 have delivered:
    - React Router v7 shell with Today / History / Journal tab navigation
    - Nothing Phone design system: Geist Mono font, dot grid background, tight border-radius
    - Dark/light mode toggle with localStorage persistence and flash prevention
    - Supabase schema migration SQL and client setup (applied in Plan 02 checkpoint)
    - getLocalDateString() date utility
  </what-built>
  <how-to-verify>
    Open the dev server URL (shown above) and check each item:

    1. **Today empty state** — You see today's date (YYYY-MM-DD) in small uppercase mono text, and "No wins logged yet" below it
    2. **Tab navigation** — Tap/click History and Journal tabs; each shows a placeholder. Active tab icon/label is visually lighter/brighter than inactive tabs
    3. **Font** — Open DevTools -> Elements -> Computed -> search "font-family" on body. Confirm it shows "Geist Mono Variable"
    4. **Dot grid** — Look at any empty area of the page. You should see a very subtle grid of dots (like graph paper at low opacity). It should NOT be distracting
    5. **Dark mode toggle** — Click the sun/moon icon in the top-right header. The entire page switches between dark and light modes instantly
    6. **No flash on refresh** — Set to dark mode, then hard-refresh (Cmd+Shift+R on Mac). The page should open dark with NO white flash
    7. **Dark dot grid** — Confirm the dot grid is also visible in dark mode (light dots on dark background)
    8. **Persistence** — After toggling to dark mode and refreshing, the app opens in dark mode. After toggling to light and refreshing, opens in light mode

    Report any visual issues or confirm everything looks good.
  </how-to-verify>
  <resume-signal>
    Type "approved" if everything looks correct, or describe any issues (e.g., "font not loading", "dot grid too bright", "flash on refresh still happening")
  </resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when:
- All automated tests pass (Vitest green)
- User has confirmed visual acceptance of the design system
- Dark mode toggle works without flash
- Navigation works across all three routes
</verification>

<success_criteria>
Phase 1 foundation accepted. Ready to begin Phase 2 (Win Logging and Focus Tracking) in a fresh context window.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`

Then update `.planning/STATE.md`:
- status: executing (Phase 2 ready)
- stopped_at: Phase 1 complete
- progress.completed_phases: 1
</output>
