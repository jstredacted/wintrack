---
phase: 01-foundation
plan: "04"
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - src/index.css
  - index.html
  - src/hooks/useTheme.js
  - src/components/theme/ThemeToggle.jsx
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02

must_haves:
  truths:
    - "useTheme hook tests pass — toggle adds/removes .dark class and persists to localStorage"
    - "Flash-prevention script is in index.html <head> before the module script tag"
    - "Geist Mono Variable font is imported in index.css and set as --font-sans"
    - "Dot grid background is applied to body in both light and dark modes"
    - "--radius is set to 0.25rem (tight precision feel) and all radius scale tokens updated"
  artifacts:
    - path: "src/hooks/useTheme.js"
      provides: "Theme toggle hook with localStorage persistence"
      exports: ["useTheme"]
    - path: "src/components/theme/ThemeToggle.jsx"
      provides: "Sun/Moon icon button using shadcn Button"
      exports: ["ThemeToggle"]
    - path: "src/index.css"
      provides: "Design system tokens, Geist Mono font, dot grid background"
      contains: "Geist Mono Variable"
    - path: "index.html"
      provides: "Flash-prevention inline script in <head>"
      contains: "wintrack-theme"
  key_links:
    - from: "index.html inline script"
      to: "src/hooks/useTheme.js"
      via: "Both use localStorage key 'wintrack-theme' and .dark class on documentElement"
      pattern: "wintrack-theme"
    - from: "src/index.css @custom-variant dark"
      to: ".dark class on <html>"
      via: "(&:is(.dark *)) — set by both inline script and useTheme hook"
      pattern: "custom-variant dark"
---

<objective>
Implement the design system tokens, Geist Mono font, dot grid background, dark mode toggle hook, and flash-prevention script — establishing the full Nothing Phone visual identity.

Purpose: SHELL-01 (dark mode toggle) and SHELL-02 (Nothing Phone design language) are the two Phase 1 requirements. This plan closes both. The design tokens established here (font, radius, spacing scale) are the primitives all feature phases use — they must not be revisited.
Output: Complete design system in index.css, useTheme hook, ThemeToggle component, flash-free dark mode.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@src/index.css

<interfaces>
<!-- Pattern 2: Dark mode toggle (flash-free) -->
<!-- index.html inline script — runs BEFORE any module script -->
```html
<script>
  (function () {
    try {
      var stored = localStorage.getItem('wintrack-theme');
      if (stored === 'dark' || (!stored && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
        document.documentElement.classList.add('dark');
      }
    } catch (e) {}
  })();
</script>
```
<!-- Place this BEFORE <script type="module" src="/src/main.jsx"> -->

<!-- useTheme hook — reads what the inline script already applied -->
```js
import { useState, useEffect } from "react";
const STORAGE_KEY = "wintrack-theme";
export function useTheme() {
  const [theme, setTheme] = useState(() =>
    document.documentElement.classList.contains("dark") ? "dark" : "light"
  );
  useEffect(() => {
    const root = document.documentElement;
    if (theme === "dark") { root.classList.add("dark"); }
    else { root.classList.remove("dark"); }
    localStorage.setItem(STORAGE_KEY, theme);
  }, [theme]);
  const toggle = () => setTheme((t) => (t === "dark" ? "light" : "dark"));
  return { theme, toggle };
}
```

<!-- Pattern 4: Geist Mono in Tailwind v4 -->
<!-- In index.css, after existing @import "@fontsource-variable/geist": -->
<!-- @import "@fontsource-variable/geist-mono"; -->
<!-- In @theme inline block: --font-sans: 'Geist Mono Variable', monospace; -->
<!-- Font family name is exactly 'Geist Mono Variable' (capital V) -->

<!-- Pattern 5: CSS dot grid -->
<!-- Applied to body, inside @layer base or after it -->
<!-- Light: oklch(0.5 0 0 / 0.06) — dark: oklch(1 0 0 / 0.05) — 24px spacing, 1px dots -->
<!-- Use background-attachment: fixed to prevent scroll jank -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement useTheme hook and ThemeToggle component</name>
  <files>src/hooks/useTheme.js, src/components/theme/ThemeToggle.jsx</files>
  <behavior>
    - toggle() adds .dark class to document.documentElement when theme is light
    - toggle() removes .dark class from document.documentElement when theme is dark
    - toggle() sets localStorage key "wintrack-theme" to "dark" or "light"
    - Initial state reads from document.documentElement.classList (which the inline script set)
    - If .dark class is on <html>, initial theme is "dark"
    - If .dark class is absent, initial theme is "light"
  </behavior>
  <action>
    Create `src/hooks/useTheme.js` exactly as shown in the interfaces block above.

    Create `src/components/theme/ThemeToggle.jsx`:
    ```jsx
    import { Sun, Moon } from "lucide-react";
    import { Button } from "@/components/ui/button";
    import { useTheme } from "@/hooks/useTheme";

    export default function ThemeToggle() {
      const { theme, toggle } = useTheme();
      return (
        <Button
          variant="ghost"
          size="icon"
          onClick={toggle}
          aria-label={theme === "dark" ? "Switch to light mode" : "Switch to dark mode"}
        >
          {theme === "dark" ? <Sun className="size-4" /> : <Moon className="size-4" />}
        </Button>
      );
    }
    ```

    Run the useTheme tests after creating useTheme.js — they should turn GREEN (tests were written in Plan 01).

    Critical: The localStorage key must be exactly `"wintrack-theme"` — must match the inline script in index.html (added in Task 2).
  </action>
  <verify>
    <automated>cd /Users/justin/Repositories/Personal/wintrack && /Users/justin/.local/share/mise/installs/node/22.22.0/bin/node node_modules/.bin/vitest run src/hooks/useTheme.test.js --reporter=verbose</automated>
  </verify>
  <done>All 3 useTheme tests pass (green); useTheme.js and ThemeToggle.jsx exist</done>
</task>

<task type="auto">
  <name>Task 2: Apply design system to index.css and add flash-prevention to index.html</name>
  <files>src/index.css, index.html</files>
  <action>
    Install Geist Mono font package:
    ```
    npm install @fontsource-variable/geist-mono
    ```

    Update `src/index.css` with these changes (modify the existing file — do not replace the entire content, only the described sections):

    1. **Add Geist Mono import** — after the existing `@import "@fontsource-variable/geist";` line:
       ```css
       @import "@fontsource-variable/geist-mono";
       ```

    2. **Update `--font-sans` in `@theme inline`** — change from `'Geist Variable'` to:
       ```css
       --font-sans: 'Geist Mono Variable', monospace;
       ```

    3. **Tighten border-radius token** — in `:root`, change `--radius: 0.625rem` to:
       ```css
       --radius: 0.25rem;
       ```
       This gives the precision/tight feel of the Nothing Phone design language. The derived tokens in `@theme inline` (--radius-sm, --radius-md, etc.) automatically scale from this value.

    4. **Add dot grid background** — inside `@layer base`, update the `body` rule:
       ```css
       body {
         @apply bg-background text-foreground;
         background-image: radial-gradient(circle, oklch(0.5 0 0 / 0.06) 1px, transparent 1px);
         background-size: 24px 24px;
         background-attachment: fixed;
       }
       ```

    5. **Add dark mode dot grid override** — after the `@layer base` block (or inside it, after the body rule):
       ```css
       .dark body {
         background-image: radial-gradient(circle, oklch(1 0 0 / 0.05) 1px, transparent 1px);
         background-size: 24px 24px;
         background-attachment: fixed;
       }
       ```

    Update `index.html` — add the flash-prevention inline script in `<head>` BEFORE the `<script type="module">` tag:
    ```html
    <script>
      (function () {
        try {
          var stored = localStorage.getItem('wintrack-theme');
          if (stored === 'dark' || (!stored && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark');
          }
        } catch (e) {}
      })();
    </script>
    ```

    The final `<head>` section should be:
    ```html
    <head>
      <meta charset="UTF-8" />
      <link rel="icon" type="image/svg+xml" href="/vite.svg" />
      <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
      <title>wintrack</title>
      <script>
        (function () {
          try {
            var stored = localStorage.getItem('wintrack-theme');
            if (stored === 'dark' || (!stored && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
              document.documentElement.classList.add('dark');
            }
          } catch (e) {}
        })();
      </script>
    </head>
    ```

    Note: `viewport-fit=cover` is added to the viewport meta — this enables `env(safe-area-inset-bottom)` to work correctly for iPhone home indicator clearance (used in BottomTabBar from Plan 03).
  </action>
  <verify>
    <automated>cd /Users/justin/Repositories/Personal/wintrack && /Users/justin/.local/share/mise/installs/node/22.22.0/bin/node node_modules/.bin/vitest run --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    - index.css contains `@import "@fontsource-variable/geist-mono"`
    - index.css `--font-sans` is `'Geist Mono Variable', monospace`
    - index.css `--radius` is `0.25rem`
    - index.css body rule includes the radial-gradient dot grid
    - index.html has the flash-prevention inline script before the module script
    - All Vitest tests still pass (0 regressions)
  </done>
</task>

</tasks>

<verification>
After both tasks complete:
- `npx vitest run src/hooks/useTheme.test.js` — 3 tests pass
- `npx vitest run` — full suite green (date tests from Plan 02 may still fail if Plan 02 not yet run — that is expected)
- `grep "Geist Mono Variable" src/index.css` — returns match
- `grep "wintrack-theme" index.html` — returns match
- `grep "radial-gradient" src/index.css` — returns match
</verification>

<success_criteria>
- useTheme hook tests are green
- Geist Mono Variable is set as the global font
- Dot grid background renders in both light and dark modes
- Flash-prevention script prevents FOUC on hard refresh
- ThemeToggle component is ready for Header.jsx (Plan 03) to render
- --radius tightened to 0.25rem for precision design feel
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
