---
phase: 04-history-and-journal
plan: 05
type: execute
wave: 3
depends_on:
  - 04-03
  - 04-04
files_modified: []
autonomous: false

requirements:
  - JOURNAL-01
  - JOURNAL-02
  - JOURNAL-03
  - HISTORY-01
  - HISTORY-02

must_haves:
  truths:
    - "User can create a journal entry — form opens, title+body entered, Save clicked, entry appears in list"
    - "User can edit a journal entry — Pencil clicked, form pre-fills, Save updates card in place"
    - "User can delete a journal entry — Trash clicked, entry removed from list"
    - "Journal list shows most recent entry first; empty state shows when list is empty"
    - "History tab shows heatmap with 84 day cells"
    - "Clicking a heatmap cell loads that day's wins below with Completed/Incomplete badges"
    - "Full automated test suite is GREEN before visual checkpoint"
  artifacts:
    - path: "src/pages/JournalPage.jsx"
      provides: "Working journal CRUD page"
    - path: "src/pages/HistoryPage.jsx"
      provides: "Heatmap + DayDetail interactive history page"
    - path: "src/components/journal/JournalEntryForm.jsx"
      provides: "Journal create/edit form"
    - path: "src/components/journal/JournalEntryCard.jsx"
      provides: "Journal entry display card"
    - path: "src/components/history/Heatmap.jsx"
      provides: "84-day completion heatmap"
    - path: "src/components/history/DayDetail.jsx"
      provides: "Per-day win list with badges"
  key_links:
    - from: "App router"
      to: "src/pages/JournalPage.jsx"
      via: "/journal route (already in AppShell)"
      pattern: "JournalPage"
    - from: "App router"
      to: "src/pages/HistoryPage.jsx"
      via: "/history route (already in AppShell)"
      pattern: "HistoryPage"
---

<objective>
Visual acceptance checkpoint for Phase 4. Before triggering this plan, the automated test suite must be fully GREEN. The checkpoint verifies the complete Journal and History features work end-to-end in the running app with real Supabase data.

Purpose: Catch any visual or interaction issues that automated tests cannot verify — layout, animation, empty states, Supabase live data behavior.
Output: User confirms Phase 4 features are correct. Phase 4 complete.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-history-and-journal/04-03-SUMMARY.md
@.planning/phases/04-history-and-journal/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full test suite green gate</name>
  <files></files>
  <action>
Run the complete test suite and confirm all tests pass before proceeding to visual verification. This ensures no regressions from Wave 2 parallel execution (04-03 and 04-04 may have touched overlapping test infrastructure).

Expected: All prior phase tests (useStreak, useCheckin, WinInputOverlay, etc.) still GREEN. All 7 new Phase 4 test files GREEN.

If any tests fail, diagnose and fix before proceeding to the visual checkpoint.
  </action>
  <verify>
    <automated>cd /Users/justin/Repositories/Personal/wintrack && mise exec -- npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>Full test suite exits 0 (all GREEN). No failing tests. Output shows pass counts for all test files including the 7 new Phase 4 test files.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual acceptance — Journal and History end-to-end</name>
  <what-built>
Phase 4 complete — Journal CRUD (JOURNAL-01, JOURNAL-02, JOURNAL-03) and History view (HISTORY-01, HISTORY-02).

Journal tab: create/edit/delete journal entries with title + body, inline form, list sorted by date.
History tab: 84-day heatmap grid + clickable day detail showing wins with Completed/Incomplete badges.
  </what-built>
  <how-to-verify>
Start the dev server:
```
mise exec -- npm run dev
```
Visit http://localhost:5174 (or 5173 if 5174 is not available).

**Journal verification (JOURNAL-01, JOURNAL-02, JOURNAL-03):**
1. Click the Journal tab in the bottom navigation
2. Verify: empty state shows "No entries yet" if no entries exist
3. Click "New Entry" — verify inline form appears with Title input and body textarea
4. Type a title and body, click Save — verify entry appears in the list
5. Click the Pencil (edit) icon on the entry — verify inline form appears pre-filled with the title and body
6. Change the title, click Save — verify the card updates in place with the new title
7. Click Trash (delete) icon on an entry — verify it disappears from the list
8. Create two entries — verify most recent appears first

**History verification (HISTORY-01, HISTORY-02):**
1. Click the History tab
2. Verify: heatmap grid renders with approximately 84 small square cells
3. Days where you completed a check-in should appear visually distinct (brighter/filled) vs empty days
4. Click a cell for a day you know has wins — verify that day's wins appear below with Completed/Incomplete badges
5. Click a cell for today or a recent day — verify it shows the correct wins
6. Verify the design matches Nothing Phone aesthetic: monospaced font, black/white, no color

**Overall:**
- No console errors
- Animations feel right (entry list fade in/out)
- Design is consistent with the rest of the app (Today view, Check-in overlay)
  </how-to-verify>
  <resume-signal>Type "approved" if everything looks correct, or describe any issues to fix</resume-signal>
  <files></files>
  <action>Visually verify Journal and History features in the running app. Start dev server: mise exec -- npm run dev</action>
  <verify>
    <automated>mise exec -- npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>User types "approved" confirming Journal CRUD and History heatmap work correctly in the browser.</done>
</task>

</tasks>

<verification>
Phase 4 is complete when:
1. Full test suite GREEN (Task 1)
2. Visual acceptance approved (Task 2 checkpoint)

Requirements covered:
- JOURNAL-01: create entry ✓
- JOURNAL-02: edit and delete ✓
- JOURNAL-03: list view sorted by date ✓
- HISTORY-01: browse past day wins with completion status ✓
- HISTORY-02: visual heatmap of completed days ✓
</verification>

<success_criteria>
- All 7 Phase 4 test files GREEN
- All prior phase tests still GREEN (no regressions)
- User approves journal create/edit/delete flow
- User approves heatmap rendering and day-detail click interaction
- User approves design consistency with Nothing Phone aesthetic
</success_criteria>

<output>
After completion, create `.planning/phases/04-history-and-journal/04-05-SUMMARY.md`

Also update ROADMAP.md Phase 4 status to complete and STATE.md to reflect Phase 4 done.
</output>
