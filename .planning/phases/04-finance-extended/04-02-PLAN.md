---
phase: 04-finance-extended
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/finance/BillRow.tsx
  - src/components/finance/BillsList.tsx
  - src/components/finance/BillAddInline.tsx
  - src/pages/FinancePage.tsx
autonomous: true
requirements:
  - BILL-02
  - BILL-03
  - BILL-05
  - BILL-06
  - BILL-07
must_haves:
  truths:
    - "User sees bills as compact rows sorted by due date urgency (overdue first)"
    - "User can toggle a bill as paid and see balance deducted"
    - "Paid one-time bills disappear from the active list"
    - "Recurring bills show a repeat icon and remaining count on tap"
    - "User can quick-add a bill from the Finance page"
  artifacts:
    - path: "src/components/finance/BillRow.tsx"
      provides: "Compact bill row with urgency border and paid toggle"
      contains: "BillRow"
    - path: "src/components/finance/BillsList.tsx"
      provides: "Bills list sorted by urgency, filters paid one-time"
      contains: "BillsList"
    - path: "src/components/finance/BillAddInline.tsx"
      provides: "Inline add bill form"
      contains: "BillAddInline"
  key_links:
    - from: "src/components/finance/BillsList.tsx"
      to: "src/hooks/useBills.ts"
      via: "togglePaid callback prop"
      pattern: "togglePaid|onTogglePaid"
    - from: "src/pages/FinancePage.tsx"
      to: "src/components/finance/BillsList.tsx"
      via: "JSX render"
      pattern: "<BillsList"
---

<objective>
Build the bills UI: compact bill rows with urgency indicators, sorted list with paid one-time filtering, and inline add form. Wire into FinancePage.

Purpose: Bills are the core feature of Phase 4. This plan creates the visible bill management on the Finance page.
Output: BillRow, BillsList, BillAddInline components wired into FinancePage.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-finance-extended/04-CONTEXT.md
@.planning/phases/04-finance-extended/04-UI-SPEC.md
@.planning/phases/04-finance-extended/04-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output -->

From src/types/finance.ts:
```typescript
export type RecurrenceType = 'one_time' | 'recurring_n' | 'recurring_until' | 'ongoing';
export interface MonthlyBill {
  id: string; month_id: string; bill_template_id: string;
  name: string; amount: number; due_day: number;
  paid: boolean; paid_at: string | null;
  bill_templates?: BillTemplate;
}
export interface BillTemplate {
  id: string; name: string; amount: number; due_day: number;
  recurrence_type: RecurrenceType; recurrence_count: number | null;
  recurrence_end: string | null; start_month: string; active: boolean;
}
```

From src/lib/utils/finance.ts:
```typescript
export type UrgencyLevel = 'overdue' | 'urgent' | 'soon' | 'normal';
export interface DueUrgency { level: UrgencyLevel; label: string; daysUntil: number; }
export function getDueUrgency(dueDay: number, today?: Date): DueUrgency;
```

From src/hooks/useBills.ts:
```typescript
export function useBills(monthId: string | null): {
  bills: MonthlyBill[]; loading: boolean; error: string | null;
  togglePaid: (id: string, paid: boolean) => Promise<void>;
  addBill: (template: {...}) => Promise<void>;
  deleteBill: (templateId: string) => Promise<void>;
}
```

From src/lib/utils/currency.ts:
```typescript
export function formatPHP(amount: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BillRow and BillsList components</name>
  <files>
    src/components/finance/BillRow.tsx
    src/components/finance/BillsList.tsx
  </files>
  <read_first>
    src/components/finance/IncomeCard.tsx
    src/components/finance/BillRow.tsx
    src/types/finance.ts
    src/lib/utils/finance.ts
    src/lib/utils/currency.ts
    .planning/phases/04-finance-extended/04-UI-SPEC.md
  </read_first>
  <action>
    **BillRow.tsx:**
    Compact row component. Props: `bill: MonthlyBill`, `onTogglePaid: (id: string, paid: boolean) => void`, `readOnly: boolean`.

    Layout (single row, flex items-center):
    - Left urgency border: use `getDueUrgency(bill.due_day)` to determine level. CSS classes by level:
      - normal: `border-l-2 border-foreground/20`
      - soon: `border-l-2 border-foreground/40`
      - urgent: `border-l-3 border-foreground/70`
      - overdue: `border-l-3 border-destructive`
    - If paid: override border to `border-l-2 border-foreground/20` (muted)
    - Content (flex-1, flex items-center gap-2, py-4 px-4):
      - Repeat icon from Lucide (`Repeat`, size 14, text-muted-foreground) ONLY if `bill.bill_templates?.recurrence_type !== 'one_time'`
      - Bill name: `text-base font-mono` + if paid: `line-through opacity-60`
      - Spacer (flex-1)
      - Due date label: `text-[0.778rem] text-muted-foreground` showing urgency.label (hide if paid)
      - Amount: `text-[1.333rem] font-mono tabular-nums` using formatPHP(bill.amount)
      - Paid toggle: button/checkbox area. If paid: filled circle (CheckCircle2 from Lucide). If unpaid: empty circle (Circle). On click: call onTogglePaid(bill.id, !bill.paid). Disable if readOnly.
    - Background: `bg-card rounded-lg`
    - Expand on tap (if recurring): useState for expanded. Show remaining count text below main row: "X months remaining" calculated from `recurrence_count - (count of monthly_bills for this template)`. For simplicity, just show recurrence info text: "Recurring - N total months" or "Ongoing" or "One-time".

    **BillsList.tsx:**
    Container component. Props: `bills: MonthlyBill[]`, `onTogglePaid: (id: string, paid: boolean) => void`, `readOnly: boolean`, `onAddBill: (template: any) => Promise<void>`, `monthId: string`.

    Logic:
    - Filter: exclude bills where `bill_templates?.recurrence_type === 'one_time' && paid === true`
    - Sort: by urgency (overdue first, then smallest daysUntil first). Use `getDueUrgency(bill.due_day).daysUntil` for sorting. Paid bills sort to bottom.
    - Render section header: `<h2 className="font-mono text-[0.778rem] uppercase tracking-widest text-muted-foreground">BILLS</h2>`
    - Empty state: "No bills this month" heading + "Add your first bill to start tracking expenses." body
    - Map filtered+sorted bills to BillRow
    - Render BillAddInline at bottom (only if not readOnly)
  </action>
  <verify>
    <automated>grep -c "getDueUrgency" src/components/finance/BillRow.tsx && grep -c "BillsList" src/components/finance/BillsList.tsx && grep "one_time.*paid" src/components/finance/BillsList.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "border-l-2 border-foreground/20" src/components/finance/BillRow.tsx` returns match (normal urgency)
    - `grep "border-l-3 border-destructive" src/components/finance/BillRow.tsx` returns match (overdue urgency)
    - `grep "line-through opacity-60" src/components/finance/BillRow.tsx` returns match (paid state)
    - `grep "getDueUrgency" src/components/finance/BillRow.tsx` returns match
    - `grep "Repeat" src/components/finance/BillRow.tsx` returns match (Lucide icon)
    - `grep "one_time" src/components/finance/BillsList.tsx` returns match (filter logic)
    - `grep "daysUntil" src/components/finance/BillsList.tsx` returns match (sort logic)
    - `grep "No bills this month" src/components/finance/BillsList.tsx` returns match (empty state)
  </acceptance_criteria>
  <done>BillRow renders compact rows with urgency borders, paid toggle, repeat icon, and expand-on-tap for recurring info. BillsList sorts by urgency, filters paid one-time, shows empty state.</done>
</task>

<task type="auto">
  <name>Task 2: BillAddInline + FinancePage integration</name>
  <files>
    src/components/finance/BillAddInline.tsx
    src/pages/FinancePage.tsx
  </files>
  <read_first>
    src/pages/FinancePage.tsx
    src/components/finance/BillsList.tsx
    src/hooks/useBills.ts
    src/hooks/useFinance.ts
    src/lib/utils/month.ts
    .planning/phases/04-finance-extended/04-UI-SPEC.md
  </read_first>
  <action>
    **BillAddInline.tsx:**
    Props: `onAddBill: (template: { name: string; amount: number; due_day: number; recurrence_type: RecurrenceType; start_month: string }) => Promise<void>`.

    States: `expanded` (boolean), `saving` (boolean), form fields: name, amount, dueDay, recurrenceType.

    Collapsed: "+ Add Bill" text button (`text-[0.778rem] text-muted-foreground hover:text-foreground cursor-pointer py-2`)

    Expanded: single-row form (flex gap-2 items-center):
    - Name input: `flex-1 bg-transparent border-b border-foreground/20 font-mono text-base py-1 focus:outline-none focus:border-foreground/50 placeholder:text-muted-foreground` placeholder="Bill name"
    - Amount input: `w-24 bg-transparent border-b border-foreground/20 font-mono text-base py-1 text-right tabular-nums focus:outline-none` placeholder="0.00" type="number"
    - Due day input: `w-16 bg-transparent border-b border-foreground/20 font-mono text-base py-1 text-center focus:outline-none` placeholder="Day" type="number" min=1 max=31
    - Recurrence select: `bg-transparent border-b border-foreground/20 font-mono text-[0.778rem] py-1 focus:outline-none` with options: One-time, Recurring, Ongoing
    - Submit on Enter key in any field. Validate: name required, amount > 0, dueDay 1-31.
    - Escape key collapses form and resets fields.
    - After successful submit: collapse form, reset fields.
    - `start_month` derived from current month using `getCurrentMonth()`.

    **FinancePage.tsx integration:**
    - Import BillsList, useBills (or use the bills from useFinance if returned there)
    - After BudgetGauge section, add BillsList section
    - Pass bills, togglePaid, addBill, readOnly (isPastMonth), monthId
    - The FinancePage layout order becomes: BalanceHero > BudgetGauge > BillsList > Income section
    - For future months: hide BillsList entirely (future months show only projected balance per CONTEXT.md -- but that's Plan 04's concern; for now just show bills if they exist)
  </action>
  <verify>
    <automated>grep -c "BillAddInline" src/components/finance/BillAddInline.tsx && grep "BillsList" src/pages/FinancePage.tsx && grep "Add Bill" src/components/finance/BillAddInline.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "\\+ Add Bill" src/components/finance/BillAddInline.tsx` returns match
    - `grep "recurrence_type" src/components/finance/BillAddInline.tsx` returns match
    - `grep "getCurrentMonth" src/components/finance/BillAddInline.tsx` returns match
    - `grep "Escape" src/components/finance/BillAddInline.tsx` returns match
    - `grep "BillsList" src/pages/FinancePage.tsx` returns match
    - `grep "import.*BillsList" src/pages/FinancePage.tsx` returns match
    - `bun run test -- --run` passes (no regressions)
  </acceptance_criteria>
  <done>BillAddInline renders inline form with name/amount/due day/recurrence. FinancePage shows BillsList section between BudgetGauge and Income. Quick-add works with Enter to submit and Escape to cancel.</done>
</task>

</tasks>

<verification>
- Bills section visible on Finance page with proper section header
- Bill rows show urgency borders (overdue=destructive, urgent=70%, soon=40%, normal=20%)
- Paid bills show strikethrough + opacity reduction
- Paid one-time bills filtered out of list
- Recurring bills show Repeat icon
- "+ Add Bill" expands inline form, Enter submits, Escape cancels
- `bun run test -- --run` passes
</verification>

<success_criteria>
Complete bills UI on Finance page: urgency-sorted compact rows with paid toggle, one-time filtering, recurring icons, and inline add form.
</success_criteria>

<output>
After completion, create `.planning/phases/04-finance-extended/04-02-SUMMARY.md`
</output>
