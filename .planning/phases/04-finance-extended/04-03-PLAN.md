---
phase: 04-finance-extended
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/finance/BalanceHistoryIndicator.tsx
  - src/components/finance/BalanceHistoryModal.tsx
  - src/components/finance/OneOffIncomeSection.tsx
  - src/components/finance/OneOffIncomeRow.tsx
  - src/pages/FinancePage.tsx
autonomous: true
requirements:
  - HIST-02
  - HIST-03
  - HIST-04
  - ONEOFF-01
  - ONEOFF-03
  - ONEOFF-04
must_haves:
  truths:
    - "User sees last balance change indicator below BalanceHero"
    - "User can open a history modal showing all balance changes for the month"
    - "User can delete a balance change entry, reverting balance"
    - "User can add one-off income with amount, date, and note"
    - "User can edit and delete one-off income entries"
  artifacts:
    - path: "src/components/finance/BalanceHistoryIndicator.tsx"
      provides: "Greyed-out last-change delta text, clickable"
      contains: "BalanceHistoryIndicator"
    - path: "src/components/finance/BalanceHistoryModal.tsx"
      provides: "Modal listing all balance changes with revert"
      contains: "BalanceHistoryModal"
    - path: "src/components/finance/OneOffIncomeSection.tsx"
      provides: "One-off income section with inline add"
      contains: "OneOffIncomeSection"
    - path: "src/components/finance/OneOffIncomeRow.tsx"
      provides: "Single one-off entry row with edit/delete"
      contains: "OneOffIncomeRow"
  key_links:
    - from: "src/components/finance/BalanceHistoryIndicator.tsx"
      to: "src/hooks/useBalanceHistory.ts"
      via: "lastChange prop from hook"
      pattern: "lastChange"
    - from: "src/components/finance/BalanceHistoryModal.tsx"
      to: "src/hooks/useBalanceHistory.ts"
      via: "revertChange callback"
      pattern: "revertChange|revert_balance_change"
    - from: "src/components/finance/OneOffIncomeSection.tsx"
      to: "src/hooks/useOneOffIncome.ts"
      via: "addEntry/deleteEntry callbacks"
      pattern: "addEntry|deleteEntry"
---

<objective>
Build balance history UI (indicator + modal with revert) and one-off income UI (section with inline add, row with edit/delete). Wire both into FinancePage.

Purpose: Balance history provides audit trail and revert for manual overrides. One-off income lets users record bonuses/receivables.
Output: BalanceHistoryIndicator, BalanceHistoryModal, OneOffIncomeSection, OneOffIncomeRow components integrated into FinancePage.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-finance-extended/04-CONTEXT.md
@.planning/phases/04-finance-extended/04-UI-SPEC.md
@.planning/phases/04-finance-extended/04-01-SUMMARY.md

<interfaces>
From src/types/finance.ts:
```typescript
export interface BalanceChange {
  id: string; month_id: string; old_balance: number; new_balance: number;
  delta: number; note: string | null; created_at: string;
}
export interface OneOffIncome {
  id: string; month_id: string; amount: number; date: string; note: string;
  created_at: string; updated_at: string;
}
```

From src/hooks/useBalanceHistory.ts:
```typescript
export function useBalanceHistory(monthId: string | null): {
  changes: BalanceChange[]; lastChange: BalanceChange | null;
  loading: boolean; error: string | null;
  revertChange: (id: string) => Promise<void>;
  refetch: () => void;
}
```

From src/hooks/useOneOffIncome.ts:
```typescript
export function useOneOffIncome(monthId: string | null): {
  entries: OneOffIncome[]; loading: boolean; error: string | null;
  addEntry: (amount: number, date: string, note: string) => Promise<void>;
  deleteEntry: (id: string) => Promise<void>;
  updateEntry: (id: string, fields: Partial<{amount: number; date: string; note: string}>) => Promise<void>;
  refetch: () => void;
}
```

From src/lib/utils/currency.ts:
```typescript
export function formatPHP(amount: number): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BalanceHistoryIndicator + BalanceHistoryModal</name>
  <files>
    src/components/finance/BalanceHistoryIndicator.tsx
    src/components/finance/BalanceHistoryModal.tsx
  </files>
  <read_first>
    src/components/finance/BalanceHero.tsx
    src/components/wins/WinInputOverlay.jsx
    src/index.css
    src/lib/utils/currency.ts
    .planning/phases/04-finance-extended/04-UI-SPEC.md
  </read_first>
  <action>
    **BalanceHistoryIndicator.tsx:**
    Props: `lastChange: BalanceChange | null`, `onClick: () => void`.

    - If lastChange is null: render nothing (return null)
    - Otherwise: render clickable text below BalanceHero
    - Layout: `text-center cursor-pointer`
    - Text: `text-[0.778rem] text-muted-foreground font-mono`
    - Content: `{delta > 0 ? '+' : ''}${formatPHP(lastChange.delta)} from last change`
    - Example: "+P30,000 from last change" or "-P5,000 from last change"
    - On click: call onClick (opens modal)

    **BalanceHistoryModal.tsx:**
    Props: `changes: BalanceChange[]`, `onRevert: (id: string) => Promise<void>`, `onClose: () => void`, `open: boolean`.

    - Use portal to body (same pattern as WinInputOverlay: `createPortal(..., document.body)`)
    - Use overlay-slide-in / overlay-slide-out keyframe animation pattern from WinInputOverlay (visible/exiting useState + onAnimationEnd)
    - Overlay: `fixed inset-0 z-50 bg-background/95`
    - Header: "Balance History" heading + close X button (X icon from Lucide)
    - Content area: `max-w-[600px] mx-auto px-8 py-12 space-y-3`
    - Empty state: "No balance changes recorded"
    - Each change row (`bg-card rounded-lg p-4 flex items-center gap-3`):
      - Date: `text-[0.778rem] text-muted-foreground font-mono` using `new Date(change.created_at).toLocaleDateString()`
      - Delta: `text-[1.333rem] font-mono tabular-nums` with sign. Positive = normal text, negative = text-destructive
      - Result: `text-[0.778rem] text-muted-foreground` showing "Balance: {formatPHP(change.new_balance)}"
      - Note (if present): `text-[0.778rem] text-muted-foreground italic`
      - Revert button: right-aligned, initially shows trash icon (Trash2 from Lucide, text-muted-foreground hover:text-destructive)
    - Revert confirmation: when user clicks revert on a row, replace that row's content with inline confirmation:
      - "Revert will adjust balance by {formatPHP(-change.delta)}. This cannot be undone."
      - Two buttons: "Revert" (text-destructive) / "Keep Change" (text-muted-foreground)
      - On confirm: call onRevert(change.id), then the row disappears
  </action>
  <verify>
    <automated>grep "from last change" src/components/finance/BalanceHistoryIndicator.tsx && grep "revert_balance_change\|onRevert\|Revert" src/components/finance/BalanceHistoryModal.tsx && grep "createPortal" src/components/finance/BalanceHistoryModal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "from last change" src/components/finance/BalanceHistoryIndicator.tsx` returns match
    - `grep "text-muted-foreground" src/components/finance/BalanceHistoryIndicator.tsx` returns match (greyed out per CONTEXT.md)
    - `grep "formatPHP" src/components/finance/BalanceHistoryIndicator.tsx` returns match
    - `grep "createPortal" src/components/finance/BalanceHistoryModal.tsx` returns match (portal pattern)
    - `grep "overlay-slide-in\|overlay-enter" src/components/finance/BalanceHistoryModal.tsx` returns match (animation)
    - `grep "Revert" src/components/finance/BalanceHistoryModal.tsx` returns match (confirmation)
    - `grep "Keep Change" src/components/finance/BalanceHistoryModal.tsx` returns match (safe dismiss)
    - `grep "This cannot be undone" src/components/finance/BalanceHistoryModal.tsx` returns match (destructive copy)
  </acceptance_criteria>
  <done>BalanceHistoryIndicator shows last change delta as greyed-out clickable text. BalanceHistoryModal renders full-screen overlay with change list, per-row revert confirmation using exact UI-SPEC copy.</done>
</task>

<task type="auto">
  <name>Task 2: OneOffIncomeSection + OneOffIncomeRow + FinancePage wiring</name>
  <files>
    src/components/finance/OneOffIncomeSection.tsx
    src/components/finance/OneOffIncomeRow.tsx
    src/pages/FinancePage.tsx
  </files>
  <read_first>
    src/pages/FinancePage.tsx
    src/components/finance/IncomeCard.tsx
    src/components/journal/JournalEntryCard.tsx
    src/hooks/useOneOffIncome.ts
    src/hooks/useBalanceHistory.ts
    .planning/phases/04-finance-extended/04-UI-SPEC.md
  </read_first>
  <action>
    **OneOffIncomeRow.tsx:**
    Props: `entry: OneOffIncome`, `onDelete: (id: string) => Promise<void>`, `onUpdate: (id: string, fields: Partial<...>) => Promise<void>`, `readOnly: boolean`.

    Compact row (`bg-card rounded-lg p-4 flex items-center gap-3 group`):
    - Note text: `text-base font-mono flex-1`
    - Date: `text-[0.778rem] text-muted-foreground font-mono` formatted as short date
    - Amount: `text-[1.333rem] font-mono tabular-nums` using formatPHP
    - Actions (hover reveal, following JournalEntryCard pattern): `opacity-0 group-hover:opacity-100 transition-opacity flex gap-1`
      - Edit button (Pencil icon from Lucide, 14px, text-muted-foreground hover:text-foreground)
      - Delete button (Trash2 icon, 14px, text-muted-foreground hover:text-destructive)
    - Delete confirmation inline: "This will remove {formatPHP(entry.amount)} from your current balance." with Delete/Cancel buttons
    - Edit mode: replace note and amount with inline inputs (same border-b pattern as BillAddInline), Enter saves, Escape cancels
    - If readOnly: hide action buttons

    **OneOffIncomeSection.tsx:**
    Props: `entries: OneOffIncome[]`, `onAdd: (amount: number, date: string, note: string) => Promise<void>`, `onDelete: (id: string) => Promise<void>`, `onUpdate: (id: string, fields: Partial<...>) => Promise<void>`, `readOnly: boolean`.

    - Section header: `<h2 className="font-mono text-[0.778rem] uppercase tracking-widest text-muted-foreground">ONE-OFF INCOME</h2>`
    - Empty state: "No bonus income" heading + "Record one-off income like freelance payments or debts collected." body
    - List of OneOffIncomeRow entries
    - Inline add form (if not readOnly): "Add Income" trigger text, expands to form with:
      - Amount input (number, w-24)
      - Date input (date type)
      - Note input (text, flex-1)
      - Enter to submit, Escape to cancel
      - Default date: today

    **FinancePage.tsx wiring:**
    - Import BalanceHistoryIndicator, BalanceHistoryModal, OneOffIncomeSection
    - Import useBalanceHistory, useOneOffIncome hooks
    - Call `useBalanceHistory(monthData?.id ?? null)` and `useOneOffIncome(monthData?.id ?? null)`
    - Add state for historyModalOpen (boolean)
    - Place BalanceHistoryIndicator directly below BalanceHero (before BudgetGauge)
    - Place OneOffIncomeSection below the Income section
    - Render BalanceHistoryModal when historyModalOpen is true
    - After balance/income/bill mutations succeed, refetch balance history too
    - Layout order: BalanceHero > BalanceHistoryIndicator > BudgetGauge > [BillsList from Plan 02] > Income > OneOffIncomeSection
    - Note: BillsList may or may not be present yet (Plan 02 runs in parallel). If it's not imported yet, that's fine -- Plan 02 will add it. Just ensure OneOffIncomeSection goes after Income section.
  </action>
  <verify>
    <automated>grep "OneOffIncomeSection" src/pages/FinancePage.tsx && grep "BalanceHistoryIndicator" src/pages/FinancePage.tsx && grep "BalanceHistoryModal" src/pages/FinancePage.tsx && grep "useBalanceHistory" src/pages/FinancePage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ONE-OFF INCOME" src/components/finance/OneOffIncomeSection.tsx` returns match
    - `grep "No bonus income" src/components/finance/OneOffIncomeSection.tsx` returns match
    - `grep "Add Income" src/components/finance/OneOffIncomeSection.tsx` returns match
    - `grep "group-hover:opacity" src/components/finance/OneOffIncomeRow.tsx` returns match (hover reveal)
    - `grep "This will remove" src/components/finance/OneOffIncomeRow.tsx` returns match (delete confirmation)
    - `grep "BalanceHistoryIndicator" src/pages/FinancePage.tsx` returns match
    - `grep "BalanceHistoryModal" src/pages/FinancePage.tsx` returns match
    - `grep "OneOffIncomeSection" src/pages/FinancePage.tsx` returns match
    - `grep "useBalanceHistory" src/pages/FinancePage.tsx` returns match
    - `grep "useOneOffIncome" src/pages/FinancePage.tsx` returns match
    - `bun run test -- --run` passes
  </acceptance_criteria>
  <done>BalanceHistoryIndicator, BalanceHistoryModal, OneOffIncomeSection, OneOffIncomeRow all created and wired into FinancePage. History indicator shows below hero, modal has revert confirmation, one-off section has inline add with hover-reveal edit/delete.</done>
</task>

</tasks>

<verification>
- Balance history indicator visible below BalanceHero when changes exist
- History modal opens with all changes, each has revert with confirmation
- One-off income section visible below Income with proper header
- Inline add form works with Enter/Escape
- Hover-reveal edit/delete on one-off rows
- Delete confirmation shows amount being removed
- `bun run test -- --run` passes
</verification>

<success_criteria>
Balance history and one-off income fully visible and functional on Finance page. Indicator greyed-out per design. Modal uses portal + slide animation. One-off section follows established section header pattern.
</success_criteria>

<output>
After completion, create `.planning/phases/04-finance-extended/04-03-SUMMARY.md`
</output>
