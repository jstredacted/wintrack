---
phase: 04-finance-extended
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - src/hooks/useYearOverview.ts
  - src/components/finance/YearGrid.tsx
  - src/components/finance/MonthColumn.tsx
  - src/components/finance/BalanceSparkline.tsx
  - src/pages/YearOverviewPage.tsx
  - src/pages/SettingsPage.tsx
  - src/App.tsx
autonomous: false
requirements:
  - FIN-05
  - ONEOFF-03
  - BILL-01
  - BILL-03
  - BILL-04
  - BILL-07
  - HIST-01
  - ONEOFF-02
must_haves:
  truths:
    - "User can navigate to /finance/year and see 12-month columns with balance, income-vs-expense bars, one-off extensions"
    - "User can tap a month column to navigate to that month's detailed view"
    - "Balance sparkline shows trajectory across 12 months"
    - "User can manage bill templates (add/edit/delete) in Settings"
    - "Visual checkpoint confirms all finance features working end-to-end"
  artifacts:
    - path: "src/pages/YearOverviewPage.tsx"
      provides: "Year overview page at /finance/year"
      contains: "YearOverviewPage"
    - path: "src/components/finance/YearGrid.tsx"
      provides: "12-column grid layout"
      contains: "YearGrid"
    - path: "src/components/finance/MonthColumn.tsx"
      provides: "Single month column with progress bar"
      contains: "MonthColumn"
    - path: "src/components/finance/BalanceSparkline.tsx"
      provides: "SVG sparkline for balance trajectory"
      contains: "BalanceSparkline"
    - path: "src/hooks/useYearOverview.ts"
      provides: "12-month aggregated data hook"
      contains: "useYearOverview"
  key_links:
    - from: "src/hooks/useYearOverview.ts"
      to: "supabase RPC get_year_overview"
      via: "supabase.rpc call"
      pattern: "supabase\\.rpc\\('get_year_overview'"
    - from: "src/components/finance/MonthColumn.tsx"
      to: "/finance?month="
      via: "navigation on click"
      pattern: "navigate.*month=|/finance\\?month="
    - from: "src/App.tsx"
      to: "src/pages/YearOverviewPage.tsx"
      via: "route definition"
      pattern: "finance/year"
---

<objective>
Build the year overview page (/finance/year) with 12-month grid, progress bars with one-off income extensions, balance sparkline. Add bill template management to Settings page. Visual checkpoint for full phase verification.

Purpose: Year overview is the dashboard view showing the full financial picture. Settings bill management completes the CRUD story. Checkpoint ensures all Phase 4 features work.
Output: YearOverviewPage with YearGrid/MonthColumn/BalanceSparkline, Settings bill editor, visual verification.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-finance-extended/04-CONTEXT.md
@.planning/phases/04-finance-extended/04-UI-SPEC.md
@.planning/phases/04-finance-extended/04-01-SUMMARY.md
@.planning/phases/04-finance-extended/04-02-SUMMARY.md
@.planning/phases/04-finance-extended/04-03-SUMMARY.md
@.planning/phases/04-finance-extended/04-04-SUMMARY.md

<interfaces>
From src/types/finance.ts:
```typescript
export interface MonthSummary {
  month: string; ending_balance: number; starting_balance: number;
  total_income: number; total_expenses: number; total_oneoff: number;
}
export interface BillTemplate {
  id: string; name: string; amount: number; due_day: number;
  recurrence_type: RecurrenceType; recurrence_count: number | null;
  recurrence_end: string | null; start_month: string; active: boolean;
}
```

From src/lib/utils/currency.ts:
```typescript
export function formatPHP(amount: number): string;
```

From src/App.tsx:
```typescript
// Routes inside PinGate > AppShell children
{ index: true, Component: TodayPage },
{ path: "journal", Component: JournalPage },
{ path: "finance", Component: FinancePage },
{ path: "settings", Component: SettingsPage },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useYearOverview hook + YearGrid + MonthColumn + BalanceSparkline + YearOverviewPage + Route</name>
  <files>
    src/hooks/useYearOverview.ts
    src/components/finance/YearGrid.tsx
    src/components/finance/MonthColumn.tsx
    src/components/finance/BalanceSparkline.tsx
    src/pages/YearOverviewPage.tsx
    src/App.tsx
  </files>
  <read_first>
    src/App.tsx
    src/hooks/useFinance.ts
    src/components/finance/BudgetGauge.tsx
    src/components/finance/MonthStrip.tsx
    src/pages/FinancePage.tsx
    src/lib/utils/currency.ts
    .planning/phases/04-finance-extended/04-UI-SPEC.md
  </read_first>
  <action>
    **src/hooks/useYearOverview.ts:**
    - Accept `year: number`
    - Call `supabase.rpc('get_year_overview', { p_user_id: USER_ID, p_year: year })`
    - Returns `{ months: MonthSummary[], loading: boolean, error: string | null }`
    - If RPC returns fewer than 12 months, fill in missing months with projected data (or zeros for months that don't exist yet)

    **src/components/finance/MonthColumn.tsx:**
    Props: `summary: MonthSummary`, `isCurrent: boolean`, `onClick: () => void`.

    Layout (flex flex-col items-center, clickable, px-2):
    - Month label: `text-[0.778rem] font-mono text-muted-foreground uppercase` showing abbreviated month name (Jan, Feb, etc.)
    - Ending balance: `text-[1.333rem] font-mono tabular-nums` using formatPHP. Abbreviate large numbers: P150K, P1.2M.
    - Vertical progress bar (the signature visual):
      - Container: `w-full h-32 bg-card rounded relative overflow-hidden`
      - Income fill: from bottom, height = `(totalExpenses / totalIncome) * 100%` of container, background = `bg-foreground/30` (expenses consuming income)
      - One-off extension: if total_oneoff > 0, extend beyond the income fill at `bg-foreground/15` (lower opacity per CONTEXT.md)
      - The bar shows consumption: income is the full bar height, expenses fill from bottom showing how much was consumed
    - Current month indicator: if isCurrent, add `border border-foreground/30` to the column
    - On click: navigate to `/finance?month=${summary.month}`
    - Hover: `hover:bg-card/80` or subtle background change

    **src/components/finance/BalanceSparkline.tsx:**
    Props: `months: MonthSummary[]`.

    Raw SVG sparkline:
    - Width: 100% (viewBox responsive)
    - Height: 48px
    - Single `<path>` element connecting ending_balance values across months
    - Stroke: `currentColor` (foreground), strokeWidth 1.5, fill none
    - Scale Y from min to max balance across months
    - If fewer than 2 data points: render nothing

    **src/components/finance/YearGrid.tsx:**
    Props: `months: MonthSummary[]`, `onMonthClick: (month: string) => void`.

    Layout:
    - `grid grid-cols-12 gap-1` on desktop
    - Map months to MonthColumn
    - Current month detection: compare each month string to getCurrentMonth()

    **src/pages/YearOverviewPage.tsx:**
    - Import useYearOverview, YearGrid, BalanceSparkline
    - State: year (default current year)
    - Call useYearOverview(year)
    - Layout within `max-w-[1100px] mx-auto px-8 py-12`:
      - Header: flex items-center justify-between
        - "Year Overview" heading (`text-[1.333rem] font-mono font-light`)
        - Back link to /finance: text button with ArrowLeft icon
      - BalanceSparkline (48px height, full width)
      - YearGrid (12 columns)
    - On MonthColumn click: navigate to `/finance?month=YYYY-MM`
    - Use `useNavigate` from react-router

    **src/App.tsx:**
    - Add lazy import for YearOverviewPage
    - Add route `{ path: "finance/year", Component: YearOverviewPage }` inside AppShell children
    - Place before the "finance" route to avoid conflicts
  </action>
  <verify>
    <automated>grep "get_year_overview" src/hooks/useYearOverview.ts && grep "grid-cols-12" src/components/finance/YearGrid.tsx && grep "<path" src/components/finance/BalanceSparkline.tsx && grep "finance/year" src/App.tsx && grep "Year Overview" src/pages/YearOverviewPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "get_year_overview" src/hooks/useYearOverview.ts` returns match
    - `grep "MonthSummary" src/hooks/useYearOverview.ts` returns match
    - `grep "grid-cols-12" src/components/finance/YearGrid.tsx` returns match
    - `grep "bg-foreground/30" src/components/finance/MonthColumn.tsx` returns match (income fill)
    - `grep "bg-foreground/15" src/components/finance/MonthColumn.tsx` returns match (one-off extension)
    - `grep "<path" src/components/finance/BalanceSparkline.tsx` returns match (SVG path)
    - `grep "strokeWidth.*1.5\|stroke-width.*1.5" src/components/finance/BalanceSparkline.tsx` returns match
    - `grep "finance/year" src/App.tsx` returns match (route)
    - `grep "Year Overview" src/pages/YearOverviewPage.tsx` returns match
    - `grep "useNavigate\|navigate" src/pages/YearOverviewPage.tsx` returns match
  </acceptance_criteria>
  <done>Year overview page at /finance/year shows 12-month grid with progress bars (income fill + one-off extension), balance sparkline, and clickable month columns that navigate to detailed view. Route registered in App.tsx.</done>
</task>

<task type="auto">
  <name>Task 2: Settings bill template management</name>
  <files>
    src/pages/SettingsPage.tsx
  </files>
  <read_first>
    src/pages/SettingsPage.tsx
    src/components/finance/IncomeSourceEditor.tsx
    src/types/finance.ts
    src/hooks/useBills.ts
    .planning/phases/04-finance-extended/04-UI-SPEC.md
  </read_first>
  <action>
    Add a "Bills" management section to SettingsPage, following the same pattern as IncomeSourceEditor.

    **Implementation:**
    - Import BillTemplate type and add a useBillTemplates hook (or inline the supabase queries if simpler)
    - Fetch bill_templates: `supabase.from('bill_templates').select('*').eq('user_id', USER_ID).eq('active', true).order('due_day')`
    - Add section below Income Sources section with header: `<h2 className="font-mono text-[0.778rem] uppercase tracking-widest text-muted-foreground">BILLS</h2>`

    **Bill template list:**
    Each template row shows: name, amount (formatPHP), due day, recurrence type label, edit/delete buttons.
    - Row: `bg-card rounded-lg p-4 flex items-center gap-3`
    - Name: `text-base font-mono flex-1`
    - Amount: `text-[1.333rem] font-mono tabular-nums`
    - Due day: `text-[0.778rem] text-muted-foreground` "Due {day}{ordinal}"
    - Recurrence: `text-[0.778rem] text-muted-foreground` badge-like text: "One-time" / "Recurring (N months)" / "Until YYYY-MM" / "Ongoing"
    - Edit button: Pencil icon (14px, text-muted-foreground)
    - Delete button: Trash2 icon (14px, text-muted-foreground hover:text-destructive)

    **Add/Edit form:**
    Toggle between list and form view (same pattern as IncomeSourceEditor).
    Form fields:
    - Name: text input
    - Amount: number input
    - Due Day: number input (1-31)
    - Recurrence Type: select (One-time / Recurring for N months / Recurring until date / Ongoing)
    - If recurring_n: show "Number of months" number input
    - If recurring_until: show "End month" input (YYYY-MM format)
    - Start Month: auto-filled with current month for new templates
    - Save/Cancel buttons

    **Delete confirmation:**
    Inline text: "This will remove the bill template and all future unpaid instances. Paid instances are preserved." with Delete/Cancel buttons.
    On delete: `supabase.from('bill_templates').update({ active: false }).eq('id', templateId)` (soft delete to preserve monthly_bills history).
  </action>
  <verify>
    <automated>grep "BILLS" src/pages/SettingsPage.tsx && grep "bill_templates" src/pages/SettingsPage.tsx && grep "recurrence_type" src/pages/SettingsPage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "BILLS" src/pages/SettingsPage.tsx` returns match (section header)
    - `grep "bill_templates" src/pages/SettingsPage.tsx` returns match (table query)
    - `grep "recurrence_type" src/pages/SettingsPage.tsx` returns match (form field)
    - `grep "One-time\|one_time" src/pages/SettingsPage.tsx` returns match
    - `grep "Recurring\|recurring_n" src/pages/SettingsPage.tsx` returns match
    - `grep "Ongoing\|ongoing" src/pages/SettingsPage.tsx` returns match
    - `grep "This will remove the bill template" src/pages/SettingsPage.tsx` returns match (delete confirmation)
    - `bun run test -- --run` passes
  </acceptance_criteria>
  <done>Settings page has full bill template management: list with edit/delete, add form with recurrence options, delete confirmation with soft-delete to preserve history.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of all Phase 4 features</name>
  <files>src/pages/FinancePage.tsx, src/pages/YearOverviewPage.tsx, src/pages/SettingsPage.tsx</files>
  <action>
    Start dev server with `mise exec -- bun run dev` and verify all Phase 4 features end-to-end. This is a human verification checkpoint -- no code changes needed, just visual and functional testing of everything built in Plans 01-05.
  </action>
  <verify>
    <automated>bun run test -- --run</automated>
  </verify>
  <what-built>
    Complete Finance Extended phase:
    1. Bills management with compact rows, urgency borders, paid toggle, inline add
    2. Balance change history with indicator, modal, and revert
    3. One-off income with inline add, hover-reveal edit/delete
    4. Waterfall step chart (SVG) for current and past months
    5. Month view modes (current interactive, past read-only, future projected)
    6. Year overview at /finance/year with 12-column grid, progress bars, sparkline
    7. Settings bill template CRUD
  </what-built>
  <how-to-verify>
    Start dev server: `mise exec -- bun run dev`

    **Bills (Finance page):**
    1. Navigate to Finance page
    2. Add a bill using "+ Add Bill" inline form (name: "Rent", amount: 15000, due day: 1, recurrence: Ongoing)
    3. Add another bill (name: "Netflix", amount: 549, due day: 15, recurrence: Ongoing)
    4. Add a one-time bill (name: "Doctor visit", amount: 2000, due day: 20, recurrence: One-time)
    5. Verify bills sorted by urgency (nearest due date first)
    6. Verify urgency borders (check border thickness/color for different due dates)
    7. Toggle "Rent" as paid -- verify strikethrough, opacity, balance deducted
    8. Toggle "Doctor visit" as paid -- verify it disappears from list (one-time + paid)

    **Waterfall chart:**
    9. Verify SVG step chart visible below BudgetGauge
    10. Paid bills show solid line steps, unpaid show dashed
    11. Balance cascades downward with each bill

    **Balance history:**
    12. Override balance using BalanceHero tap-to-edit
    13. Verify greyed-out indicator appears below hero (e.g., "+P30,000 from last change")
    14. Click indicator to open history modal
    15. Verify modal shows the change with date, delta, resulting balance
    16. Test revert: click delete on a change, verify confirmation text, click "Revert"

    **One-off income:**
    17. Scroll to "ONE-OFF INCOME" section
    18. Add one-off income (amount: 5000, note: "Freelance project")
    19. Verify it appears in the list
    20. Hover over entry -- verify edit/delete buttons appear
    21. Delete entry -- verify confirmation and balance adjustment

    **Month navigation:**
    22. Navigate to a past month via MonthStrip
    23. Verify "Past month -- read only" indicator, all controls disabled
    24. Navigate to a future month
    25. Verify single projected balance number with descriptive text

    **Year overview:**
    26. Navigate to /finance/year
    27. Verify 12-month columns with month labels and balances
    28. Verify progress bars show income consumption
    29. Verify sparkline in header area
    30. Click a month column -- verify navigation to /finance?month=YYYY-MM

    **Settings:**
    31. Go to Settings page
    32. Verify "BILLS" section with bill templates listed
    33. Edit a template (change amount)
    34. Delete a template (verify confirmation text)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>All Phase 4 features visually verified and approved by user.</done>
</task>

</tasks>

<verification>
- /finance/year route loads and shows 12-month grid
- Each MonthColumn shows progress bar with income/expense visualization
- One-off income extends progress bar at lower opacity
- Sparkline renders balance trajectory
- Settings has full bill template CRUD
- All Phase 4 requirements verified visually
</verification>

<success_criteria>
Year overview page complete at /finance/year. Settings bill management complete. All Phase 4 features visually verified and approved.
</success_criteria>

<output>
After completion, create `.planning/phases/04-finance-extended/04-05-SUMMARY.md`
</output>
