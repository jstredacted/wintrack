---
phase: 04-finance-extended
plan: 04
type: execute
wave: 3
depends_on: ["04-02", "04-03"]
files_modified:
  - src/components/finance/WaterfallChart.tsx
  - src/pages/FinancePage.tsx
autonomous: true
requirements:
  - FIN-02
  - FIN-03
  - FIN-04
must_haves:
  truths:
    - "Current month shows SVG waterfall step chart with balance cascading as bills are paid"
    - "Past months show full snapshot (bills, income, budget) as read-only"
    - "Future months show single projected balance number"
  artifacts:
    - path: "src/components/finance/WaterfallChart.tsx"
      provides: "SVG step chart showing balance cascade"
      contains: "WaterfallChart"
  key_links:
    - from: "src/components/finance/WaterfallChart.tsx"
      to: "src/lib/utils/finance.ts"
      via: "buildWaterfallSteps"
      pattern: "buildWaterfallSteps"
    - from: "src/pages/FinancePage.tsx"
      to: "src/components/finance/WaterfallChart.tsx"
      via: "JSX render for current/past months"
      pattern: "<WaterfallChart"
---

<objective>
Build the waterfall step chart SVG and implement month view modes (current month with waterfall, past months read-only with waterfall, future months with projected balance only).

Purpose: The waterfall chart is the signature visualization of the finance page, showing how balance cascades as bills are paid. Month modes differentiate current vs past vs future views.
Output: WaterfallChart component, FinancePage updated with month mode logic.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-finance-extended/04-CONTEXT.md
@.planning/phases/04-finance-extended/04-UI-SPEC.md
@.planning/phases/04-finance-extended/04-01-SUMMARY.md
@.planning/phases/04-finance-extended/04-02-SUMMARY.md
@.planning/phases/04-finance-extended/04-03-SUMMARY.md

<interfaces>
From src/lib/utils/finance.ts:
```typescript
export interface WaterfallStep {
  name: string; amount: number; paid: boolean; balanceAfter: number;
}
export function buildWaterfallSteps(startingBalance: number, bills: { name: string; amount: number; due_day: number; paid: boolean }[]): WaterfallStep[];
```

From src/types/finance.ts:
```typescript
export interface MonthlyBill {
  id: string; name: string; amount: number; due_day: number;
  paid: boolean; bill_templates?: BillTemplate;
}
```

From src/lib/utils/currency.ts:
```typescript
export function formatPHP(amount: number): string;
```

From src/lib/utils/month.ts:
```typescript
export function getCurrentMonth(): string;
export function getNextMonth(month: string): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: WaterfallChart SVG component</name>
  <files>
    src/components/finance/WaterfallChart.tsx
  </files>
  <read_first>
    src/components/finance/BudgetGauge.tsx
    src/components/history/ConsistencyGraph.tsx
    src/lib/utils/finance.ts
    src/lib/utils/currency.ts
    .planning/phases/04-finance-extended/04-UI-SPEC.md
  </read_first>
  <action>
    Create raw SVG step chart component. Props: `startingBalance: number`, `bills: MonthlyBill[]`, `isPastMonth: boolean`.

    Implementation:
    - Use `buildWaterfallSteps(startingBalance, bills)` to compute step data
    - SVG dimensions: width 100% of container (use viewBox for responsive), height 200px
    - Padding: top 20px (for top label), bottom 40px (for bill name labels), left 60px (for balance labels), right 16px
    - Y-axis: scale from 0 to startingBalance (or max step balance). Use linear scale.
    - X-axis: divide available width equally among steps + 1 (starting point)

    Drawing:
    - Starting point: horizontal line at startingBalance level from left edge to first step position
    - For each step: draw horizontal line at previous balance level, then vertical drop to new balance level, then horizontal line to next step position
    - Paid steps: `stroke="currentColor"` (inherits foreground), strokeWidth 2, solid
    - Unpaid steps: `stroke="currentColor"` with `opacity="0.3"`, strokeDasharray="4 4"
    - Past months (isPastMonth=true): all steps solid (everything finalized)

    Labels:
    - Y-axis: starting balance at top, ending balance at bottom. `text-[0.778rem] font-mono tabular-nums fill-current opacity-50`. Use formatPHP but abbreviated (e.g., "P150K" for 150000, "P1.2M" for 1200000). For simplicity, just use formatPHP with no abbreviation if space allows.
    - X-axis: bill names below each step, rotated 0 degrees (horizontal). `text-[0.778rem] font-mono fill-current opacity-50`. Truncate long names to ~8 chars with ellipsis.
    - Optional: small circles at each step point for visual anchoring

    Edge cases:
    - No bills: render nothing (or a flat line at startingBalance)
    - All bills paid: show full cascade
    - Zero starting balance: y-axis bottoms out at 0

    No interactivity (read-only visualization per CONTEXT.md).
  </action>
  <verify>
    <automated>grep "buildWaterfallSteps" src/components/finance/WaterfallChart.tsx && grep "viewBox\|<svg" src/components/finance/WaterfallChart.tsx && grep "strokeDasharray\|stroke-dasharray" src/components/finance/WaterfallChart.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "buildWaterfallSteps" src/components/finance/WaterfallChart.tsx` returns match
    - `grep "<svg" src/components/finance/WaterfallChart.tsx` returns match (raw SVG)
    - `grep "strokeDasharray\|stroke-dasharray" src/components/finance/WaterfallChart.tsx` returns match (unpaid dashed lines)
    - `grep "formatPHP\|formatAmount" src/components/finance/WaterfallChart.tsx` returns match (Y-axis labels)
    - `grep "WaterfallChart" src/components/finance/WaterfallChart.tsx` returns match (exported)
    - No charting library imports (no recharts, d3, visx)
  </acceptance_criteria>
  <done>WaterfallChart renders raw SVG step chart with solid paid steps and dashed unpaid steps. Y-axis shows balance amounts, X-axis shows bill names. Responsive via viewBox.</done>
</task>

<task type="auto">
  <name>Task 2: FinancePage month view modes (current/past/future)</name>
  <files>
    src/pages/FinancePage.tsx
  </files>
  <read_first>
    src/pages/FinancePage.tsx
    src/hooks/useFinance.ts
    src/hooks/useBills.ts
    src/lib/utils/month.ts
    .planning/phases/04-finance-extended/04-UI-SPEC.md
  </read_first>
  <action>
    Update FinancePage to handle three month modes:

    **Determine month mode:**
    ```typescript
    const currentMonth = getCurrentMonth();
    const isCurrentMonth = selectedMonth === currentMonth;
    const isPastMonth = selectedMonth < currentMonth;
    const isFutureMonth = selectedMonth > currentMonth;
    ```

    **Current month view (isCurrentMonth):**
    Full layout as currently built:
    1. MonthStrip
    2. BalanceHero (editable)
    3. BalanceHistoryIndicator
    4. BudgetGauge (editable)
    5. WaterfallChart (with paid=solid, unpaid=dashed)
    6. BillsList (with toggle + add)
    7. Income section (with toggle)
    8. OneOffIncomeSection (with add/edit/delete)

    **Past month view (isPastMonth):**
    Full snapshot, all read-only:
    1. MonthStrip
    2. Past month indicator: "Past month -- read only" (`text-center text-[0.778rem] text-muted-foreground py-2`)
    3. BalanceHero (readOnly=true, balance shows final balance)
    4. BalanceHistoryIndicator (still clickable to view history)
    5. BudgetGauge (readOnly=true)
    6. WaterfallChart (isPastMonth=true, all steps solid)
    7. BillsList (readOnly=true, no toggle, no add)
    8. Income section (readOnly=true)
    9. OneOffIncomeSection (readOnly=true)

    **Future month view (isFutureMonth):**
    Simple projected view:
    1. MonthStrip
    2. Projected balance display: single large number showing `projected = currentMonthEndingBalance + expectedIncome - recurringBillsTotal`
       - Calculate: fetch or compute projected balance. For simplicity, use monthData.current_balance (since ensure_month_exists carries forward) + sum of expected income amounts - sum of bill template amounts for recurring/ongoing bills.
       - Display: `text-[2.667rem] font-light font-mono tabular-nums text-center` (Display size from UI-SPEC)
       - Below: "Projected balance based on expected income and recurring bills" in `text-[0.778rem] text-muted-foreground text-center`
    3. Nothing else (no waterfall, no bills, no income cards per CONTEXT.md)

    **Expand months list to include future months:**
    Currently, MonthStrip shows 6 months back + current (7 total). Extend to also include 3 months forward (10 total). Update the months useMemo to generate: 6 back + current + 3 forward.
  </action>
  <verify>
    <automated>grep "isFutureMonth" src/pages/FinancePage.tsx && grep "Projected balance" src/pages/FinancePage.tsx && grep "WaterfallChart" src/pages/FinancePage.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "isFutureMonth" src/pages/FinancePage.tsx` returns match
    - `grep "isPastMonth" src/pages/FinancePage.tsx` returns match
    - `grep "isCurrentMonth" src/pages/FinancePage.tsx` returns match
    - `grep "Projected balance based on" src/pages/FinancePage.tsx` returns match (future month copy)
    - `grep "WaterfallChart" src/pages/FinancePage.tsx` returns match
    - `grep "readOnly" src/pages/FinancePage.tsx` returns match (past month)
    - Future months show only projected balance, not full layout
    - `bun run test -- --run` passes
  </acceptance_criteria>
  <done>FinancePage renders three distinct views: current (full interactive with waterfall), past (full read-only snapshot with solid waterfall), future (projected balance only). MonthStrip extended with 3 future months.</done>
</task>

</tasks>

<verification>
- Current month shows waterfall with paid (solid) and unpaid (dashed) steps
- Past months show all-solid waterfall in read-only mode
- Future months show single projected balance number with descriptive text
- MonthStrip includes 3 future months
- No charting library dependencies
- `bun run test -- --run` passes
</verification>

<success_criteria>
Waterfall step chart visualizes balance cascade. Three month view modes working correctly. Future months show clean projected balance without clutter.
</success_criteria>

<output>
After completion, create `.planning/phases/04-finance-extended/04-04-SUMMARY.md`
</output>
