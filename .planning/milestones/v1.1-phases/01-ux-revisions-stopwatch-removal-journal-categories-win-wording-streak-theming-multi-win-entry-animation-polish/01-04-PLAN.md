---
phase: 01-ux-revisions
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/components/layout/SideNav.jsx
  - src/components/layout/StreakCelebration.jsx
autonomous: true
requirements: [UX-06, UX-07]
must_haves:
  truths:
    - "Streak display uses a Lucide Flame icon, not a fire emoji"
    - "Streak celebration does NOT auto-close after a timeout"
    - "Streak celebration shows 'You're on a roll' text"
    - "User must click to dismiss the streak celebration"
  artifacts:
    - path: "src/components/layout/SideNav.jsx"
      provides: "Monochrome streak icon with Lucide Flame"
    - path: "src/components/layout/StreakCelebration.jsx"
      provides: "Click-to-dismiss celebration with tagline"
  key_links:
    - from: "src/components/layout/SideNav.jsx"
      to: "lucide-react"
      via: "Flame icon import"
      pattern: "import.*Flame.*lucide-react"
---

<objective>
Replace the fire emoji streak display with a monochrome Lucide Flame icon and make the streak celebration require an explicit click to dismiss (remove auto-close, add "you're on a roll" messaging).

Purpose: Fire emoji breaks the Nothing Phone monochrome aesthetic. The celebration auto-closes too fast for users to enjoy the moment.
Output: Monochrome Flame icon in SideNav, persistent celebration overlay requiring click dismiss.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-ux-revisions-stopwatch-removal-journal-categories-win-wording-streak-theming-multi-win-entry-animation-polish/01-RESEARCH.md

<interfaces>
From src/components/layout/SideNav.jsx (line 82-88):
```jsx
{!loading && combinedStreak > 0 && (
  <span
    className="font-mono text-xs tabular-nums"
    title={`Streak: ${combinedStreak} days`}
    aria-label={`Streak: ${combinedStreak} days`}
  >
    {combinedStreak}🔥   // <-- replace emoji with Flame icon
  </span>
)}
```

From src/components/layout/StreakCelebration.jsx:
```jsx
export default function StreakCelebration({ open, streak, onClose })
// Line 37-40: auto-close useEffect to REMOVE:
// useEffect(() => {
//   if (!open) return;
//   const t = setTimeout(onClose, 4000);
//   return () => clearTimeout(t);
// }, [open, onClose]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace fire emoji with Lucide Flame icon in SideNav</name>
  <files>src/components/layout/SideNav.jsx</files>
  <action>
1. Add import: `import { Flame } from 'lucide-react'` (Flame may already be partially imported with other icons — check existing imports first and add to the destructuring).

2. Replace the streak display (around line 88) from:
   ```jsx
   {combinedStreak}🔥
   ```
   To:
   ```jsx
   <span className="flex items-center gap-1">
     <Flame size={12} strokeWidth={1.5} />
     {combinedStreak}
   </span>
   ```

3. Keep the parent span's existing classes (`font-mono text-xs tabular-nums`), `title`, and `aria-label` attributes.
  </action>
  <verify>
    <automated>cd /Users/justin/Repositories/Personal/wintrack && grep -c "🔥" src/components/layout/SideNav.jsx; grep -c "Flame" src/components/layout/SideNav.jsx</automated>
  </verify>
  <done>Zero fire emoji matches in SideNav. Flame import present. Streak renders with monochrome Lucide icon.</done>
</task>

<task type="auto">
  <name>Task 2: Make StreakCelebration require click to dismiss</name>
  <files>src/components/layout/StreakCelebration.jsx</files>
  <action>
1. Remove the auto-close `useEffect` entirely (the one with `setTimeout(onClose, 4000)` around lines 37-40). Remove the entire useEffect block, not just the timeout value.

2. Add "You're on a roll" tagline below the streak count number. Style:
   ```jsx
   <span className="font-mono text-sm uppercase tracking-[0.2em] text-muted-foreground mt-2">
     You're on a roll
   </span>
   ```

3. Add a "tap to continue" or "click to continue" hint at the bottom. The existing "tap to continue" text may already be there — verify and keep it. If not, add:
   ```jsx
   <span className="font-mono text-[10px] uppercase tracking-[0.15em] text-muted-foreground/40 mt-8">
     tap to continue
   </span>
   ```

4. Keep the existing `onClick={onClose}` on the container div — this is the explicit dismiss mechanism.

5. Optionally extend the number ramp animation duration from 1500ms to 2500ms (in the `useEffect` with `requestAnimationFrame` — change `const duration = 1500` to `const duration = 2500`).

6. Update existing tests for StreakCelebration:
   - Remove or update any test that asserts auto-close timeout behavior
   - Add/verify test that onClose is NOT called automatically
   - Verify "You're on a roll" text is rendered
  </action>
  <verify>
    <automated>cd /Users/justin/Repositories/Personal/wintrack && mise exec -- npx vitest run src/components/layout/ --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>StreakCelebration has no setTimeout auto-close. "You're on a roll" text visible. Click dismisses. Ramp animation extended. Tests pass.</done>
</task>

</tasks>

<verification>
- `grep -rn "🔥" src/` returns zero matches
- `grep -c "setTimeout.*onClose" src/components/layout/StreakCelebration.jsx` returns 0
- `grep -c "on a roll" src/components/layout/StreakCelebration.jsx` returns 1+
- `mise exec -- npx vitest run` passes
</verification>

<success_criteria>
Streak display is fully monochrome (Lucide Flame, no emoji). Celebration overlay stays until clicked, shows encouraging message, has slower number ramp.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ux-revisions-stopwatch-removal-journal-categories-win-wording-streak-theming-multi-win-entry-animation-polish/01-04-SUMMARY.md`
</output>
