---
phase: 01-ux-revisions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/002_phase01_changes.sql
  - src/hooks/useWins.js
  - src/components/wins/WinCard.jsx
  - src/components/wins/TimerFocusOverlay.jsx
  - src/components/wins/TotalFocusTime.jsx
  - src/pages/TodayPage.jsx
  - src/hooks/useStopwatch.js
autonomous: true
requirements: [UX-01, UX-02]
must_haves:
  truths:
    - "No timer UI is visible anywhere in the app"
    - "Win cards display title only, no stopwatch controls"
    - "Adding a win no longer sends timer columns to Supabase"
    - "Roll-forward no longer sends timer columns"
    - "DB migration file exists to drop timer columns"
  artifacts:
    - path: "supabase/migrations/002_phase01_changes.sql"
      provides: "Drop timer columns, add journal category column"
      contains: "DROP COLUMN"
    - path: "src/hooks/useWins.js"
      provides: "Win CRUD without timer functions"
    - path: "src/pages/TodayPage.jsx"
      provides: "Today page without timer overlay wiring"
  key_links:
    - from: "src/pages/TodayPage.jsx"
      to: "src/hooks/useWins.js"
      via: "useWins() destructuring — no timer functions"
      pattern: "useWins.*addWin.*editWin.*deleteWin.*rollForward"
---

<objective>
Remove the stopwatch feature by commenting out all timer-related code and creating a DB migration to drop timer columns.

Purpose: The stopwatch feature is being removed from the UX. Code is commented out (not deleted) for potential re-enable. The DB migration also adds the journal category column (batching schema changes).
Output: Migration file + commented-out timer code across 6 source files.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-ux-revisions-stopwatch-removal-journal-categories-win-wording-streak-theming-multi-win-entry-animation-polish/01-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs -->

From src/hooks/useWins.js (lines to comment out):
- startTimer (line 178): async (winId) => updates timer_started_at
- pauseTimer (line 199): async (winId, displaySeconds) => updates timer_elapsed_seconds
- stopTimer (line 222): async (winId, displaySeconds) => delegates to pauseTimer
- addWin (line 79): currently includes timer_elapsed_seconds: 0, timer_started_at: null in insert
- rollForward (line 149): currently includes timer_elapsed_seconds: 0, timer_started_at: null in toInsert

From src/pages/TodayPage.jsx (lines to comment out):
- Line 9: import TimerFocusOverlay
- Line 15: import TimerFocusOverlay
- Lines 37-39: startTimer, pauseTimer, stopTimer destructuring
- Lines 45-46: openInputOverlay, closeInputOverlay (keep these — overlay still used)
- Lines 55: timerOverlayOpen
- Lines 157-161: onStartTimer, onPauseTimer, onStopTimer props
- Lines 238-260: TimerFocusOverlay JSX block

From src/components/wins/WinCard.jsx:
- Timer display span and Play/Pause/Stop button group
- useStopwatch import

From src/components/wins/TotalFocusTime.jsx:
- Entire component — timer-only display

From src/components/wins/TimerFocusOverlay.jsx:
- Entire file marked as STOPWATCH REMOVED
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DB migration and comment out timer code in hooks</name>
  <files>supabase/migrations/002_phase01_changes.sql, src/hooks/useWins.js, src/hooks/useStopwatch.js</files>
  <action>
1. Create `supabase/migrations/002_phase01_changes.sql` with:
   - DROP timer_elapsed_seconds and timer_started_at from wins table (IF EXISTS)
   - ADD category column to journal_entries (text NOT NULL DEFAULT 'daily' with CHECK constraint for 'daily', 'milestone', 'financial')

2. In `src/hooks/useWins.js`:
   - Wrap `startTimer`, `pauseTimer`, `stopTimer` function definitions in `/* STOPWATCH REMOVED */` comment blocks
   - Remove `startTimer`, `pauseTimer`, `stopTimer` from the return object (comment out)
   - In `addWin`: remove `timer_elapsed_seconds: 0` and `timer_started_at: null` from the optimistic object AND the Supabase insert payload
   - In `rollForward`: remove `timer_elapsed_seconds: 0` and `timer_started_at: null` from the `toInsert` mapping

3. In `src/hooks/useStopwatch.js`: Add a comment at top `// STOPWATCH REMOVED — file kept for potential re-enable` but do NOT delete the file contents.
  </action>
  <verify>
    <automated>grep -c "STOPWATCH REMOVED" src/hooks/useWins.js && grep -c "timer_elapsed_seconds" supabase/migrations/002_phase01_changes.sql && grep -c "category" supabase/migrations/002_phase01_changes.sql</automated>
  </verify>
  <done>Migration file contains DROP COLUMN for timer columns and ADD COLUMN for category. useWins.js no longer exports timer functions or includes timer fields in insert payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Comment out timer UI in components and TodayPage</name>
  <files>src/components/wins/WinCard.jsx, src/components/wins/TimerFocusOverlay.jsx, src/components/wins/TotalFocusTime.jsx, src/pages/TodayPage.jsx</files>
  <action>
1. In `WinCard.jsx`:
   - Comment out `useStopwatch` import
   - Comment out `displaySeconds`/`isRunning` usage
   - Comment out the timer display span and Play/Pause/Stop button group
   - Remove `onStartTimer`, `onPauseTimer`, `onStopTimer` from props destructuring (comment out)
   - Keep the win title display, edit, delete functionality intact

2. In `TimerFocusOverlay.jsx`:
   - Add comment at top: `// STOPWATCH REMOVED — entire file kept for potential re-enable`
   - Do NOT delete file contents

3. In `TotalFocusTime.jsx`:
   - Add comment at top: `// STOPWATCH REMOVED — entire component kept for potential re-enable`
   - Do NOT delete file contents

4. In `TodayPage.jsx`:
   - Comment out `import TimerFocusOverlay` (line 15)
   - Comment out `startTimer, pauseTimer, stopTimer` from useWins destructuring (lines 37-39)
   - Comment out `timerOverlayOpen` and related timer overlay state from useUIStore (line 55)
   - Comment out `onStartTimer`, `onPauseTimer`, `onStopTimer` props on WinList/WinCard (lines 157-161)
   - Comment out the entire `<TimerFocusOverlay>` JSX block (lines 238-260)
   - Comment out `import TotalFocusTime` if present
   - Keep the `openInputOverlay`/`closeInputOverlay` wiring — the win input overlay is still used
   - In the onSubmit handler for WinInputOverlay (line 192-203): remove the `startTimer(newWin.id)` call and related timer logic; keep `closeInputOverlay()` for now (Plan 02 will change this)

5. Run full test suite and fix any test failures caused by removed timer props/imports. Tests that exercise timer behavior should be commented out with `// STOPWATCH REMOVED` markers. Tests for win CRUD should still pass.
  </action>
  <verify>
    <automated>cd /Users/justin/Repositories/Personal/wintrack && mise exec -- npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>No timer UI visible in WinCard, TodayPage, or any component. All existing tests pass (timer-specific tests commented out). App renders without errors.</done>
</task>

</tasks>

<verification>
- `grep -rn "timer_elapsed_seconds\|timer_started_at" src/` returns only commented lines
- `grep -rn "STOPWATCH REMOVED" src/` shows markers in all affected files
- `mise exec -- npx vitest run` passes
- Migration file is valid SQL
</verification>

<success_criteria>
All timer/stopwatch UI is commented out. No timer columns in insert/update payloads. Migration file ready to run. Existing non-timer tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ux-revisions-stopwatch-removal-journal-categories-win-wording-streak-theming-multi-win-entry-animation-polish/01-01-SUMMARY.md`
</output>
