---
phase: 02-win-item-interactions-and-timeline-display-inline-strikethrough-timeline-style-history-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/003_add_completed_to_wins.sql
  - src/hooks/useWins.js
  - src/components/wins/WinCard.jsx
  - src/pages/TodayPage.jsx
autonomous: true
requirements: [INT-01, INT-02, INT-03]

must_haves:
  truths:
    - "User can tap a toggle button on any win card to mark it complete"
    - "Completed wins show line-through text and muted color"
    - "Toggling back to incomplete removes the strikethrough"
    - "Completion status persists to Supabase (survives refresh)"
    - "Failed Supabase update rolls back the optimistic toggle"
  artifacts:
    - path: "supabase/migrations/003_add_completed_to_wins.sql"
      provides: "completed boolean column on wins table"
      contains: "ALTER TABLE wins ADD COLUMN completed"
    - path: "src/hooks/useWins.js"
      provides: "toggleWinCompleted action"
      exports: ["useWins"]
    - path: "src/components/wins/WinCard.jsx"
      provides: "Toggle button + strikethrough rendering"
      contains: "line-through"
  key_links:
    - from: "src/components/wins/WinCard.jsx"
      to: "src/hooks/useWins.js"
      via: "onToggle prop wired to toggleWinCompleted"
      pattern: "onToggle"
    - from: "src/hooks/useWins.js"
      to: "supabase wins table"
      via: "update({ completed }) optimistic with rollback"
      pattern: "toggleWinCompleted"
---

<objective>
Add inline win completion toggle — users can mark wins complete/incomplete directly on the Today page with a circle/check button. Completed wins show line-through text. State persists to Supabase via optimistic update with rollback on error.

Purpose: Users should be able to cross off wins during the day, not just through the evening check-in.
Output: DB migration, updated useWins hook with toggleWinCompleted, WinCard with toggle affordance and strikethrough.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-win-item-interactions-and-timeline-display-inline-strikethrough-timeline-style-history-view/02-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/hooks/useWins.js:
```javascript
export function useWins() {
  // Returns: { wins, loading, error, yesterdayWins, addWin, editWin, deleteWin, rollForward }
  // wins array items have: { id, user_id, title, win_date, status, created_at }
  // Optimistic pattern: update local state first, then Supabase, rollback on error
}
```

From src/components/wins/WinCard.jsx:
```javascript
export default function WinCard({ win, onEdit, onDelete }) {
  // Currently: title row with edit/delete hover-reveal buttons
  // No toggle or completion state yet
}
```

From src/pages/TodayPage.jsx:
```javascript
// Uses useWins() hook, renders WinList with win cards
// WinList maps wins and passes onEdit/onDelete per card
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DB migration and useWins toggleWinCompleted</name>
  <files>supabase/migrations/003_add_completed_to_wins.sql, src/hooks/useWins.js</files>
  <action>
1. Create `supabase/migrations/003_add_completed_to_wins.sql`:
   ```sql
   ALTER TABLE wins ADD COLUMN completed boolean NOT NULL DEFAULT false;
   ```

2. Add `toggleWinCompleted` to `useWins.js`:
   - Accept `(id)` as single argument (find current completed value from wins array)
   - Optimistic update: flip `completed` in local state immediately
   - Supabase: `.from('wins').update({ completed: newValue }).eq('id', id).eq('user_id', USER_ID)`
   - On error: rollback to previous value, set error message
   - Add `wins` to useCallback dependency array (same pattern as rollForward which depends on yesterdayWins)
   - Add `toggleWinCompleted` to the returned object

3. In addWin optimistic object, add `completed: false` to match the new column default.

IMPORTANT: Do NOT add any auto-refresh-on-focus behavior. Current useWins fetches only on mount — this prevents stale data overwriting optimistic toggles.
  </action>
  <verify>
    <automated>mise exec -- bun run test --run</automated>
  </verify>
  <done>Migration file exists with ALTER TABLE. useWins returns toggleWinCompleted. addWin includes completed:false in optimistic object. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: WinCard toggle button and strikethrough + TodayPage wiring</name>
  <files>src/components/wins/WinCard.jsx, src/pages/TodayPage.jsx</files>
  <action>
1. Update WinCard to accept `onToggle` prop alongside existing onEdit/onDelete.

2. Add toggle button as the FIRST element in the flex row (left of title), ALWAYS visible (not hover-reveal like edit/delete):
   - Import `Circle` and `CheckCircle` from lucide-react
   - `aria-label={win.completed ? 'Mark incomplete' : 'Mark complete'}`
   - `onClick={() => onToggle?.()}`
   - Classes: `shrink-0 mt-1 text-muted-foreground/40 hover:text-foreground transition-colors`
   - Render `CheckCircle size={16}` when completed, `Circle size={16}` when not

3. Title span: conditionally apply strikethrough classes:
   - When `win.completed`: `line-through text-muted-foreground`
   - When not completed: `text-foreground` (current behavior)
   - Keep existing `flex-1 text-lg leading-snug` classes

4. Wire in TodayPage.jsx (or WinList.jsx — wherever WinCard is rendered):
   - Find where WinCard receives onEdit/onDelete props
   - Add `onToggle={() => toggleWinCompleted(win.id)}` using the new hook action
   - Ensure toggleWinCompleted is destructured from useWins()
  </action>
  <verify>
    <automated>mise exec -- bun run test --run</automated>
  </verify>
  <done>WinCard shows Circle/CheckCircle toggle left of title. Clicking toggle calls onToggle. Completed wins display line-through text-muted-foreground. TodayPage wires onToggle to toggleWinCompleted. All tests pass.</done>
</task>

</tasks>

<verification>
- `mise exec -- bun run test --run` — all existing + any new tests green
- Migration file exists at `supabase/migrations/003_add_completed_to_wins.sql`
- WinCard renders toggle button with correct aria-labels
- Completed wins show `line-through` class on title span
</verification>

<success_criteria>
- Toggle button visible on every win card (not hover-reveal)
- Clicking toggle flips completed state optimistically
- Completed wins show line-through + muted text
- Toggling back removes strikethrough
- Supabase update fires on toggle with rollback on error
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-win-item-interactions-and-timeline-display-inline-strikethrough-timeline-style-history-view/02-01-SUMMARY.md`
</output>
