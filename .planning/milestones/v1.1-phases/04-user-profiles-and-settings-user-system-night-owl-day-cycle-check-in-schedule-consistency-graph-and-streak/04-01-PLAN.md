---
phase: 04-user-profiles-and-settings
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/005_user_settings.sql
  - src/stores/settingsStore.js
  - src/hooks/useSettings.js
  - src/lib/utils/date.js
  - src/lib/utils/date.test.js
  - src/stores/settingsStore.test.js
  - src/hooks/useSettings.test.js
autonomous: true
requirements: [SETTINGS-01, SETTINGS-02, NIGHTOWL-01]

must_haves:
  truths:
    - "User settings persist to Supabase and load from localStorage cache on mount with no visible flash"
    - "settingsStore initializes from localStorage synchronously and syncs to Supabase"
    - "getLocalDateString(date, dayStartHour) returns previous calendar date when current hour < dayStartHour"
    - "useSettings hook loads from Supabase and upserts defaults if no row exists"
  artifacts:
    - path: "supabase/migrations/005_user_settings.sql"
      provides: "user_settings table with CHECK constraints and RLS"
      contains: "CREATE TABLE"
    - path: "src/stores/settingsStore.js"
      provides: "Zustand settings store with localStorage cache"
      exports: ["useSettingsStore"]
    - path: "src/hooks/useSettings.js"
      provides: "Supabase fetch/upsert hook for user settings"
      exports: ["useSettings"]
    - path: "src/lib/utils/date.js"
      provides: "Night-owl-aware getLocalDateString"
      exports: ["getLocalDateString"]
  key_links:
    - from: "src/hooks/useSettings.js"
      to: "src/stores/settingsStore.js"
      via: "setSettings call after Supabase fetch"
      pattern: "useSettingsStore.*setSettings"
    - from: "src/stores/settingsStore.js"
      to: "localStorage"
      via: "wintrack-settings key read/write"
      pattern: "wintrack-settings"
---

<objective>
Build the settings infrastructure: database table, Zustand store with localStorage cache, useSettings hook for Supabase persistence, and enhanced getLocalDateString with night-owl day boundary offset.

Purpose: Foundation for all settings-dependent features (night owl mode, configurable prompts, consistency graph).
Output: Working settings persistence layer and offset-aware date utility.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-user-profiles-and-settings-user-system-night-owl-day-cycle-check-in-schedule-consistency-graph-and-streak/04-RESEARCH.md
@.planning/phases/04-user-profiles-and-settings-user-system-night-owl-day-cycle-check-in-schedule-consistency-graph-and-streak/04-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/lib/utils/date.js:
```javascript
// Current signature (no offset):
export function getLocalDateString(date = new Date()) {
  return new Intl.DateTimeFormat("en-CA", {
    year: "numeric", month: "2-digit", day: "2-digit",
  }).format(date);
}
```

From src/lib/env.js:
```javascript
export const USER_ID = requireEnv('VITE_USER_ID');
```

From src/stores/uiStore.js:
```javascript
// Pattern: create((set) => ({...})) — Zustand v5 single-parens style
import { create } from 'zustand';
```

From src/lib/supabase.js:
```javascript
// supabase client is imported as: import { supabase } from '@/lib/supabase';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DB migration + settingsStore + useSettings hook</name>
  <files>
    supabase/migrations/005_user_settings.sql,
    src/stores/settingsStore.js,
    src/stores/settingsStore.test.js,
    src/hooks/useSettings.js,
    src/hooks/useSettings.test.js
  </files>
  <behavior>
    - settingsStore: readCache() returns null when localStorage empty, returns parsed object when populated
    - settingsStore: setSettings writes to localStorage and updates Zustand state
    - settingsStore: initial state has defaults { dayStartHour: 0, morningPromptHour: 9, eveningPromptHour: 21 }
    - useSettings: on mount, fetches from Supabase and calls setSettings
    - useSettings: upserts defaults to Supabase if no row exists (data is null/empty)
    - useSettings: returns { settings, loading, saveSettings }
    - useSettings: saveSettings calls Supabase upsert and updates store
  </behavior>
  <action>
    1. Create `supabase/migrations/005_user_settings.sql`:
       - CREATE TABLE user_settings with columns: user_id (uuid PK), day_start_hour (int DEFAULT 0, CHECK 0-6), morning_prompt_hour (int DEFAULT 9, CHECK 5-14), evening_prompt_hour (int DEFAULT 21, CHECK 17-23), created_at, updated_at
       - RLS: enable, policy for SELECT/INSERT/UPDATE where user_id = current user (same pattern as other tables using the JWT sub claim)
    2. Create `src/stores/settingsStore.js`:
       - Zustand store with create((set) => ({...})) pattern (matches uiStore.js)
       - localStorage key: 'wintrack-settings'
       - readCache() reads + JSON.parse from localStorage
       - State: { settings: readCache() ?? defaults, loaded: !!readCache(), setSettings: fn }
       - setSettings: writes to localStorage, updates state with loaded: true
    3. Write tests first for settingsStore (localStorage read/write, defaults)
    4. Create `src/hooks/useSettings.js`:
       - Imports useSettingsStore, supabase, USER_ID
       - useEffect on mount: fetch from user_settings, if data exists call setSettings, if no data upsert defaults
       - saveSettings(partial): upserts to Supabase with updated_at, calls setSettings on store
       - Returns { settings: store.settings, loading, saveSettings }
    5. Write tests for useSettings (mock supabase, verify fetch + upsert behavior)
  </action>
  <verify>
    <automated>mise exec -- bun run test --run src/stores/settingsStore.test.js src/hooks/useSettings.test.js</automated>
  </verify>
  <done>Settings store reads/writes localStorage, useSettings fetches from Supabase and upserts defaults, migration SQL ready for deployment</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enhanced getLocalDateString with night-owl offset</name>
  <files>
    src/lib/utils/date.js,
    src/lib/utils/date.test.js
  </files>
  <behavior>
    - getLocalDateString(new Date('2026-03-14T03:00:00'), 4) returns '2026-03-13' (before 4am = previous day)
    - getLocalDateString(new Date('2026-03-14T04:00:00'), 4) returns '2026-03-14' (at/after 4am = current day)
    - getLocalDateString(new Date('2026-03-14T12:00:00'), 4) returns '2026-03-14' (daytime = current day)
    - getLocalDateString(new Date('2026-03-14T12:00:00'), 0) returns '2026-03-14' (no offset = same as before)
    - getLocalDateString() with no args still works (backward compatible)
    - DST-safe: uses date.setDate(date.getDate() - 1) not millisecond subtraction for the day rollback
  </behavior>
  <action>
    1. Write failing tests first in `src/lib/utils/date.test.js` (extend existing test file if it has tests, or create new):
       - Test offset=0 returns same as before (backward compat)
       - Test offset=4, time 3am returns previous date
       - Test offset=4, time 4am returns current date
       - Test offset=4, time 12pm returns current date
       - Test offset=6, time 5:59am returns previous date
       - Test offset=0, time 1am returns current date (no offset, midnight boundary)
    2. Update `src/lib/utils/date.js`:
       - Add optional second parameter: `dayStartHour = 0`
       - When dayStartHour > 0 and current hour < dayStartHour:
         - Create new Date from input
         - Use `adjusted.setDate(adjusted.getDate() - 1)` (DST-safe, NOT millisecond subtraction)
         - Format the adjusted date
       - Otherwise format as before
       - Keep the en-CA Intl.DateTimeFormat pattern unchanged
    3. Run tests to confirm green.
  </action>
  <verify>
    <automated>mise exec -- bun run test --run src/lib/utils/date.test.js</automated>
  </verify>
  <done>getLocalDateString accepts optional dayStartHour, returns previous date when hour < offset, all existing tests still pass</done>
</task>

</tasks>

<verification>
- `mise exec -- bun run test --run` — full suite green (no regressions)
- settingsStore reads from localStorage on init, writes on setSettings
- useSettings fetches from Supabase, upserts defaults when missing
- getLocalDateString with offset=4 at 3am returns previous day's date
</verification>

<success_criteria>
- user_settings migration SQL exists with CHECK constraints
- settingsStore exports useSettingsStore with localStorage caching
- useSettings exports hook that syncs Supabase to store
- getLocalDateString backward-compatible with new dayStartHour parameter
- All new tests pass, existing tests unbroken
</success_criteria>

<output>
After completion, create `.planning/phases/04-user-profiles-and-settings-user-system-night-owl-day-cycle-check-in-schedule-consistency-graph-and-streak/04-01-SUMMARY.md`
</output>
