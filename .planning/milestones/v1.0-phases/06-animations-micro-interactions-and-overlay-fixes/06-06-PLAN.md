---
phase: 06-animations-micro-interactions-and-overlay-fixes
plan: 06
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/components/checkin/CheckInOverlay.jsx
  - src/components/checkin/MorningPrompt.jsx
  - src/components/checkin/EveningPrompt.jsx
  - src/components/layout/SideNav.jsx
  - src/pages/JournalPage.jsx
  - src/components/journal/JournalEntryCard.jsx
autonomous: true
requirements:
  - FIX-01

must_haves:
  truths:
    - "CheckInOverlay Yes/No/Next/Close buttons have scale press feedback"
    - "MorningPrompt and EveningPrompt primary and secondary buttons have scale press feedback"
    - "SideNav NavLink items have a subtle background press flash"
    - "JournalPage New Entry button has opacity press feedback"
    - "JournalEntryCard Edit and Delete hover-reveal buttons have opacity press feedback"
  artifacts:
    - path: "src/components/checkin/CheckInOverlay.jsx"
      provides: "active:scale-[0.96] transition-transform duration-75 on Yes/No/Next/Close"
    - path: "src/components/checkin/MorningPrompt.jsx"
      provides: "active:scale-[0.96] on Log a win / Later"
    - path: "src/components/checkin/EveningPrompt.jsx"
      provides: "active:scale-[0.96] on Start check-in / Later"
    - path: "src/components/layout/SideNav.jsx"
      provides: "active:bg-foreground/10 on NavLink items"
    - path: "src/pages/JournalPage.jsx"
      provides: "active:opacity-70 transition-opacity duration-75 on New Entry"
    - path: "src/components/journal/JournalEntryCard.jsx"
      provides: "active:opacity-70 transition-opacity duration-75 on Edit and Delete"
  key_links:
    - from: "CheckInOverlay buttons"
      to: "active:scale-[0.96]"
      via: "large full-width style buttons — scale model appropriate"
      pattern: "active:scale"
    - from: "SideNav NavLink"
      to: "active:bg-foreground/10"
      via: "square icon buttons — scale would misalign indicator pip"
      pattern: "active:bg-foreground"
---

<objective>
Add button press feedback to the checkin/prompt overlays, SideNav navigation, and journal surfaces. All changes are pure Tailwind class additions to existing className strings — no logic changes.

Purpose: Completes the press-feedback sweep across the remaining surfaces not covered by Plan 05. These components share no files with Plan 05, so Plans 05 and 06 can execute in parallel.
Output: 6 files with active: press feedback on every interactive button.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-animations-micro-interactions-and-overlay-fixes/06-RESEARCH.md
@.planning/phases/06-animations-micro-interactions-and-overlay-fixes/06-01-SUMMARY.md

<interfaces>
Pattern rules (from RESEARCH.md):
- Large/full-width buttons: `active:scale-[0.96] transition-transform duration-75`
- Small text/icon buttons: `active:opacity-70 transition-opacity duration-75`
- SideNav NavLink (square icon buttons): `active:bg-foreground/10` (no scale — would displace indicator pip)

CheckInOverlay.jsx button locations:
- Yes button: `"px-8 py-3 bg-foreground text-background rounded-md text-sm font-mono font-medium hover:opacity-90 transition-opacity"` → add `active:scale-[0.96] transition-transform duration-75`
- No button: `"px-8 py-3 border border-border rounded-md text-sm font-mono text-muted-foreground hover:text-foreground hover:border-foreground transition-colors"` → add `active:scale-[0.96] transition-transform duration-75`
- Next button (note form submit): `"px-6 py-2 border border-border rounded-md text-sm font-mono text-muted-foreground hover:text-foreground hover:border-foreground transition-colors"` → add `active:scale-[0.96] transition-transform duration-75`
- Close button (completion screen): `"mt-4 px-6 py-2 border border-border rounded-md text-sm font-mono text-muted-foreground hover:text-foreground hover:border-foreground transition-colors"` → add `active:scale-[0.96] transition-transform duration-75`

MorningPrompt.jsx button locations:
- Log a win button: `"w-full px-6 py-3 bg-foreground text-background rounded-md text-sm font-mono font-medium hover:opacity-90 transition-opacity"` → add `active:scale-[0.96] transition-transform duration-75`
- Later button: `"w-full px-6 py-3 border border-border rounded-md text-sm font-mono text-muted-foreground hover:text-foreground hover:border-foreground transition-colors"` → add `active:scale-[0.96] transition-transform duration-75`

EveningPrompt.jsx button locations (same structure as MorningPrompt):
- Start check-in button: same filled style → add `active:scale-[0.96] transition-transform duration-75`
- Later button: same border style → add `active:scale-[0.96] transition-transform duration-75`

SideNav.jsx NavLink — uses cn() with a function for isActive:
```jsx
className={({ isActive }) =>
  cn(
    'relative flex items-center justify-center w-10 h-10 rounded-lg transition-colors',
    isActive
      ? 'text-foreground bg-foreground/8'
      : 'text-muted-foreground hover:text-foreground hover:bg-foreground/5'
  )
}
```
Add `'active:bg-foreground/10'` to the cn() base string (applies to both active and inactive states):
```jsx
cn(
  'relative flex items-center justify-center w-10 h-10 rounded-lg transition-colors active:bg-foreground/10',
  isActive ? ... : ...
)
```

JournalPage.jsx "New Entry" button:
`"font-mono text-sm uppercase tracking-widest text-muted-foreground hover:text-foreground transition-colors border-b border-muted-foreground/40 hover:border-foreground pb-px"`
→ add `active:opacity-70 transition-opacity duration-75`

JournalEntryCard.jsx hover-reveal action buttons:
- Edit button: `"flex items-center gap-1.5 font-mono text-xs uppercase tracking-widest text-muted-foreground hover:text-foreground transition-colors"` → add `active:opacity-70 transition-opacity duration-75`
- Delete button: same → add `active:opacity-70 transition-opacity duration-75`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add active: press feedback to CheckInOverlay, MorningPrompt, EveningPrompt</name>
  <files>src/components/checkin/CheckInOverlay.jsx, src/components/checkin/MorningPrompt.jsx, src/components/checkin/EveningPrompt.jsx</files>
  <action>
    CheckInOverlay.jsx — add `active:scale-[0.96] transition-transform duration-75` to 4 buttons:
    - Yes button (bg-foreground filled)
    - No button (border style)
    - Next button (form submit, border style)
    - Close button (completion screen, border style)

    MorningPrompt.jsx — add `active:scale-[0.96] transition-transform duration-75` to 2 buttons:
    - "Log a win" button (filled bg-foreground)
    - "Later" button (border style)

    EveningPrompt.jsx — add `active:scale-[0.96] transition-transform duration-75` to 2 buttons:
    - "Start check-in" button (filled bg-foreground)
    - "Later" button (border style)

    These are large, visually heavy buttons where scale press is appropriate and intentional. The existing `transition-opacity` or `transition-colors` stays; the new `transition-transform duration-75` adds the transform transition alongside it.
  </action>
  <verify>
    <automated>mise exec -- npm run test -- --run src/components/checkin/CheckInOverlay.test.jsx src/components/checkin/MorningPrompt.test.jsx src/components/checkin/EveningPrompt.test.jsx</automated>
  </verify>
  <done>All checkin overlay tests pass. Manual: open CheckInOverlay (or MorningPrompt/EveningPrompt) — click Yes, No, and Later buttons to observe brief scale-down press.</done>
</task>

<task type="auto">
  <name>Task 2: Add active: press feedback to SideNav, JournalPage, JournalEntryCard</name>
  <files>src/components/layout/SideNav.jsx, src/pages/JournalPage.jsx, src/components/journal/JournalEntryCard.jsx</files>
  <action>
    SideNav.jsx — add `active:bg-foreground/10` to the NavLink cn() base string.
    Find the cn() call inside the NavLink className function and add to the base classes:
    ```jsx
    cn(
      'relative flex items-center justify-center w-10 h-10 rounded-lg transition-colors active:bg-foreground/10',
      isActive
        ? 'text-foreground bg-foreground/8'
        : 'text-muted-foreground hover:text-foreground hover:bg-foreground/5'
    )
    ```
    Note: Do NOT add scale — the active pip indicator (`absolute left-0 top-2 bottom-2 w-0.5`) would visually shift on scale.

    JournalPage.jsx — find the "New Entry" button and add `active:opacity-70 transition-opacity duration-75` to its className. The button currently has `transition-colors` for hover; adding `transition-opacity duration-75` alongside it is safe in Tailwind v4.

    JournalEntryCard.jsx — add `active:opacity-70 transition-opacity duration-75` to both the Edit and Delete button classNames in the hover-reveal actions div.
  </action>
  <verify>
    <automated>mise exec -- npm run test -- --run src/components/layout/SideNav.test.jsx src/pages/JournalPage.test.jsx src/components/journal/JournalEntryCard.test.jsx</automated>
  </verify>
  <done>All three test files pass. Manual: click nav items in SideNav — observe subtle bg flash on press. Click New Entry in Journal — observe opacity press. Hover over a journal entry and click Edit/Delete — observe opacity press.</done>
</task>

</tasks>

<verification>
`mise exec -- npm run test -- --run`
Full suite green.
Manual: tap through all modified components — SideNav nav items, Journal New Entry, journal card Edit/Delete, CheckInOverlay Yes/No, MorningPrompt/EveningPrompt buttons. Confirm brief visual press response on each.
</verification>

<success_criteria>
- CheckInOverlay: Yes/No/Next/Close all have active:scale-[0.96] transition-transform duration-75
- MorningPrompt: Log a win + Later have active:scale-[0.96] transition-transform duration-75
- EveningPrompt: Start check-in + Later have active:scale-[0.96] transition-transform duration-75
- SideNav NavLink: has active:bg-foreground/10 in cn() base classes
- JournalPage "New Entry": has active:opacity-70 transition-opacity duration-75
- JournalEntryCard Edit + Delete: have active:opacity-70 transition-opacity duration-75
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-animations-micro-interactions-and-overlay-fixes/06-06-SUMMARY.md`
</output>
