---
phase: 04-history-and-journal
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/hooks/useJournal.test.js
  - src/hooks/useHistory.test.js
  - src/components/journal/JournalEntryForm.test.jsx
  - src/components/journal/JournalEntryCard.test.jsx
  - src/pages/JournalPage.test.jsx
  - src/components/history/DayDetail.test.jsx
  - src/components/history/Heatmap.test.jsx
autonomous: true
requirements:
  - JOURNAL-01
  - JOURNAL-02
  - JOURNAL-03
  - HISTORY-01
  - HISTORY-02

must_haves:
  truths:
    - "All 7 test files exist and fail with module-not-found (RED), not assertion errors"
    - "Test stubs document every behavior the implementation must satisfy"
    - "Wave 1+ tasks have automated verify commands ready to use"
  artifacts:
    - path: "src/hooks/useJournal.test.js"
      provides: "JOURNAL-01 + JOURNAL-02 hook behavior stubs"
      contains: "addEntry, editEntry, deleteEntry"
    - path: "src/hooks/useHistory.test.js"
      provides: "HISTORY-01 + HISTORY-02 hook behavior stubs"
      contains: "completionMap, fetchWinsForDate"
    - path: "src/components/journal/JournalEntryForm.test.jsx"
      provides: "JOURNAL-01 form rendering stubs"
      contains: "title input, body textarea, disabled Save"
    - path: "src/components/journal/JournalEntryCard.test.jsx"
      provides: "JOURNAL-02 card stubs"
      contains: "edit button, delete button, onDelete"
    - path: "src/pages/JournalPage.test.jsx"
      provides: "JOURNAL-03 list stubs"
      contains: "entries sorted by created_at, empty state"
    - path: "src/components/history/DayDetail.test.jsx"
      provides: "HISTORY-01 day detail stubs"
      contains: "win titles, completed/incomplete badges"
    - path: "src/components/history/Heatmap.test.jsx"
      provides: "HISTORY-02 heatmap stubs"
      contains: "cell count, completed vs incomplete class"
  key_links:
    - from: "src/hooks/useJournal.test.js"
      to: "src/hooks/useJournal.js"
      via: "module import (RED until Wave 1)"
      pattern: "from.*useJournal"
    - from: "src/hooks/useHistory.test.js"
      to: "src/hooks/useHistory.js"
      via: "module import (RED until Wave 1)"
      pattern: "from.*useHistory"
---

<objective>
Create all 7 Wave 0 test stub files for Phase 4. These stubs are intentionally RED — they fail with module-not-found errors because the source files don't exist yet. They define the behavioral contract that Wave 1 and Wave 2 implementations must satisfy.

Purpose: Establish the test contract before writing implementation, following the TDD-first pattern from Phases 1-3. Wave 0 stubs prevent implementation drift and give each subsequent wave an immediate automated verify command.
Output: 7 test files. All fail with module-not-found (correct RED state).
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-history-and-journal/04-RESEARCH.md
@.planning/phases/04-history-and-journal/04-VALIDATION.md

<interfaces>
<!-- Established patterns to mirror in test stubs -->

From src/hooks/useStreak.test.js pattern (Supabase mock shape used in Phase 3):
```js
// Thenable mock builder — mockResolvedValue on .eq() breaks chained calls
// Use mock object with then/catch + all query methods as mockReturnThis()
vi.mock('@/lib/supabase', () => ({
  supabase: {
    from: vi.fn().mockReturnThis(),
    select: vi.fn().mockReturnThis(),
    eq: vi.fn().mockReturnThis(),
    order: vi.fn().mockReturnThis(),
    insert: vi.fn().mockReturnThis(),
    update: vi.fn().mockReturnThis(),
    delete: vi.fn().mockReturnThis(),
    single: vi.fn().mockReturnThis(),
    then: vi.fn((resolve) => resolve({ data: [], error: null })),
    catch: vi.fn().mockReturnThis(),
  }
}))
```

From src/hooks/useCheckin.test.js — vi.mock factory form (required to prevent supabase accessToken init error):
```js
vi.mock('@/lib/supabase', () => ({ supabase: { ... } }))
```

From src/components/checkin/CheckInOverlay.test.jsx — component test pattern:
```jsx
import { render, screen } from '@testing-library/react'
import { vi, it, expect, describe } from 'vitest'
// vi.mock supabase before component import
```

journal_entries schema (no migration needed — already exists):
- id: uuid PK
- user_id: uuid NOT NULL
- title: text NOT NULL
- body: text NOT NULL DEFAULT ''
- created_at: timestamptz NOT NULL DEFAULT now()
- updated_at: timestamptz NOT NULL DEFAULT now()
- NO entry_date column — dates derived from created_at

useHistory query shape (wins → check_ins direction):
- from('wins').select('win_date, check_ins(completed)').eq('user_id', USER_ID)
- Returns: [{ win_date: 'YYYY-MM-DD', check_ins: [{ completed: bool }] }]

fetchWinsForDate query shape:
- from('wins').select('id, title, check_ins(completed, note)').eq('user_id', USER_ID).eq('win_date', date)
- Returns: [{ id, title, check_ins: [{ completed, note }] }]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hook test stubs — useJournal and useHistory</name>
  <files>src/hooks/useJournal.test.js, src/hooks/useHistory.test.js</files>
  <action>
Create `src/hooks/useJournal.test.js` with the vi.mock factory form for '@/lib/supabase'. Write describe blocks covering:
- useJournal initial state: entries=[], loading=true then false after fetch
- addEntry: calls supabase.insert with { user_id, title, body }, adds returned row to entries state
- editEntry(id, { title, body }): calls supabase.update with { title, body, updated_at: <ISO string> }, updates entry in state optimistically; NOTE: updated_at MUST be included — no DB trigger exists
- deleteEntry(id): calls supabase.delete, removes entry from entries state

Create `src/hooks/useHistory.test.js` with vi.mock factory for '@/lib/supabase'. Write describe blocks covering:
- completionMap builds correctly: given mock wins data [{ win_date: '2026-03-09', check_ins: [{ completed: true }] }], completionMap['2026-03-09'] === true
- completionMap false for wins with no completed check_in: [{ win_date: '2026-03-08', check_ins: [{ completed: false }] }] → completionMap['2026-03-08'] === false
- fetchWinsForDate('2026-03-09'): calls supabase.from('wins').select(...).eq('win_date', '2026-03-09') and returns data array
- loading state: starts true, becomes false after fetch

Both files must use the thenable mock builder pattern (then/catch + mockReturnThis() on all query methods) — NOT mockResolvedValue on .eq() which breaks chained calls. Use vi.mock factory form to prevent supabase accessToken init error.

These tests are Wave 0 stubs — they will fail with module-not-found until Wave 1 creates the hook files. This is the correct RED state.
  </action>
  <verify>
    <automated>cd /Users/justin/Repositories/Personal/wintrack && mise exec -- npx vitest run src/hooks/useJournal.test.js src/hooks/useHistory.test.js 2>&1 | tail -20</automated>
  </verify>
  <done>Both test files exist. They fail with "Cannot find module" or similar import error (module-not-found RED), not syntax errors. Running vitest exits with code 1.</done>
</task>

<task type="auto">
  <name>Task 2: Component and page test stubs</name>
  <files>src/components/journal/JournalEntryForm.test.jsx, src/components/journal/JournalEntryCard.test.jsx, src/pages/JournalPage.test.jsx, src/components/history/DayDetail.test.jsx, src/components/history/Heatmap.test.jsx</files>
  <action>
Create `src/components/journal/JournalEntryForm.test.jsx`. Mock '@/lib/supabase' with vi.mock factory. Stubs for:
- Renders a title input (role=textbox with placeholder 'Title' or label)
- Renders a body textarea (placeholder 'Write your entry...')
- Save button is disabled when title is empty string
- Calls onSubmit({ title: 'T', body: 'B' }) when form submitted with valid title

Create `src/components/journal/JournalEntryCard.test.jsx`. Stubs for:
- Renders entry title
- Renders an edit button (getByRole('button', { name: /edit/i }) or Lucide Pencil icon)
- Renders a delete button (getByRole('button', { name: /delete/i }) or Lucide Trash icon)
- Clicking delete calls onDelete prop with entry.id
- Delete button is hidden when entry is in edit mode (editingId === entry.id)

Create `src/pages/JournalPage.test.jsx`. Mock '@/lib/supabase' factory + mock 'src/hooks/useJournal' hook or provide mock data via prop. Stubs for:
- Renders list of entries sorted by created_at DESC (most recent first)
- Renders empty state message when entries array is empty (e.g. "No entries yet")

Create `src/components/history/DayDetail.test.jsx`. Stubs for:
- Renders win titles for the selected date
- Renders "Completed" badge for wins where check_in.completed === true
- Renders "Incomplete" badge for wins where check_in.completed === false
- Renders empty state when no wins for date

Create `src/components/history/Heatmap.test.jsx`. Stubs for:
- Renders exactly N cells when given N-day window (use data-testid="heatmap-cell" or role="gridcell")
- Cell for a completed day has class/attribute distinguishing it from an incomplete day (e.g. 'bg-foreground' vs 'bg-border')
- Heatmap renders without error when completionMap is {}

All 5 test files: use vi.mock factory form where supabase is needed. These are intentionally RED — module-not-found until Wave 2 creates the source files.
  </action>
  <verify>
    <automated>cd /Users/justin/Repositories/Personal/wintrack && mise exec -- npx vitest run src/components/journal/JournalEntryForm.test.jsx src/components/journal/JournalEntryCard.test.jsx src/pages/JournalPage.test.jsx src/components/history/DayDetail.test.jsx src/components/history/Heatmap.test.jsx 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 test files exist. They fail with module-not-found (RED), not syntax errors. Directory structure src/components/journal/ and src/components/history/ exists.</done>
</task>

</tasks>

<verification>
Run full suite to confirm all 7 Wave 0 stubs are RED (module-not-found, not syntax errors):

```bash
cd /Users/justin/Repositories/Personal/wintrack && mise exec -- npx vitest run 2>&1 | grep -E "(FAIL|PASS|Cannot find|Error)"
```

Expected: 7 new test files failing with module-not-found. Existing tests (useStreak, useCheckin, etc.) must remain GREEN — Wave 0 must not break prior phases.
</verification>

<success_criteria>
- 7 test stub files exist at their specified paths
- All 7 fail with module-not-found (not syntax errors, not assertion failures)
- Existing test suite still passes (prior phase tests GREEN)
- Test stubs cover every behavior listed in the VALIDATION.md Wave 0 Requirements section
</success_criteria>

<output>
After completion, create `.planning/phases/04-history-and-journal/04-01-SUMMARY.md`
</output>
