---
phase: 07-cleanup-and-contract-fixes
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
gap_closure: true
files_modified:
  - src/components/history/Heatmap.jsx
  - src/components/history/Heatmap.test.jsx
  - src/components/journal/JournalEntryForm.jsx
  - src/components/journal/JournalEntryForm.test.jsx
  - src/hooks/useHistory.js
  - src/pages/JournalPage.jsx
  - .planning/phases/05-ux-polish/05-VERIFICATION.md
  - .planning/phases/05-ux-polish/05-05-SUMMARY.md
requirements: []

must_haves:
  truths:
    - "Heatmap.jsx and Heatmap.test.jsx are deleted"
    - "JournalEntryForm.jsx and JournalEntryForm.test.jsx are deleted"
    - "useHistory.js imports USER_ID from '@/lib/env' not import.meta.env"
    - "JournalPage passes editingId={editingId} to JournalEntryCard"
    - "Phase 5 VERIFICATION.md exists at .planning/phases/05-ux-polish/05-VERIFICATION.md"
    - "05-05-SUMMARY.md exists at .planning/phases/05-ux-polish/05-05-SUMMARY.md"
    - "All remaining tests pass (no regressions)"
---

# Plan 07-01: Cleanup & Contract Fixes

## Objective

Close all 7 tech debt items identified in the v1.0 milestone audit. Four are code changes (delete dead files, fix env import, forward missing prop); two are documentation backfills (Phase 5 VERIFICATION.md and 05-05-SUMMARY.md). All changes are mechanical and independent.

## Context

The v1.0 milestone audit (`v1.0-MILESTONE-AUDIT.md`) found no critical gaps but identified 7 tech debt items:

1. `Heatmap.jsx` + `Heatmap.test.jsx` — dead code since Phase 5 replaced Heatmap with DayStrip
2. `JournalEntryForm.jsx` + `JournalEntryForm.test.jsx` — dead code since Phase 5 replaced inline form with JournalEditorOverlay
3. `useHistory.js:4` — reads `VITE_USER_ID` via `import.meta.env.VITE_USER_ID` directly; should use `import { USER_ID } from '@/lib/env'` like every other hook
4. `JournalPage.jsx:59-63` — renders `<JournalEntryCard>` without forwarding `editingId={editingId}`, leaving the delete-suppression guard in JournalEntryCard permanently inactive
5. Phase 5 `VERIFICATION.md` missing — Phase 5 was completed and browser-approved but never received a formal verification doc
6. `05-05-SUMMARY.md` missing — Plan 05-05 was executed and approved but never received a SUMMARY.md

## Tasks

### Task 1: Delete dead Heatmap files

**Files:** `src/components/history/Heatmap.jsx`, `src/components/history/Heatmap.test.jsx`

Delete both files. `Heatmap.jsx` is imported nowhere in production code (replaced by DayStrip in Phase 5). `Heatmap.test.jsx` tests the deleted component.

Verify deletion doesn't break test suite:
```bash
npm run test -- --run
```

**Commit:** `chore(07-01): delete dead Heatmap component and test`

---

### Task 2: Delete dead JournalEntryForm files

**Files:** `src/components/journal/JournalEntryForm.jsx`, `src/components/journal/JournalEntryForm.test.jsx`

Delete both files. `JournalEntryForm.jsx` is imported nowhere in production code (replaced by JournalEditorOverlay in Phase 5). `JournalEntryForm.test.jsx` tests the deleted component.

Verify deletion doesn't break test suite:
```bash
npm run test -- --run
```

**Commit:** `chore(07-01): delete dead JournalEntryForm component and test`

---

### Task 3: Fix useHistory.js env import

**File:** `src/hooks/useHistory.js`

Change line 4 from:
```js
const USER_ID = import.meta.env.VITE_USER_ID;
```
to:
```js
import { USER_ID } from '@/lib/env';
```

Remove the `const USER_ID` line entirely and add the import at the top with the other imports.

Run tests to verify:
```bash
npm run test -- --run src/hooks/useHistory.test.js
```

**Commit:** `fix(07-01): use validated env import in useHistory (HISTORY-01, HISTORY-02)`

---

### Task 4: Forward editingId to JournalEntryCard

**File:** `src/pages/JournalPage.jsx`

Find the `<JournalEntryCard>` render call (around lines 59-63) and add `editingId={editingId}` prop:

```jsx
<JournalEntryCard
  key={entry.id}
  entry={entry}
  editingId={editingId}
  onEdit={() => handleEdit(entry)}
  onDelete={() => deleteEntry(entry.id)}
/>
```

The exact prop names may differ — read the file first and match existing prop names. The goal is to pass the `editingId` state value so `JournalEntryCard`'s `isCurrentlyEditing` guard activates correctly.

Run tests to verify:
```bash
npm run test -- --run src/pages/JournalPage.test.jsx
npm run test -- --run src/components/journal/JournalEntryCard.test.jsx
```

**Commit:** `fix(07-01): forward editingId to JournalEntryCard — activate delete guard (JOURNAL-02)`

---

### Task 5: Create Phase 5 VERIFICATION.md

**File:** `.planning/phases/05-ux-polish/05-VERIFICATION.md`

Create a verification document for Phase 5. Phase 5 is a UX polish pass with no formal requirement IDs. The phase was completed (all 5 plans executed) and features were browser-approved during Phase 6's visual acceptance checkpoint (plan 06-08).

Status should be `passed` since:
- All 5 plans have SUMMARY.md (01-04; 05-05 is backfilled in Task 6)
- Features were browser-accepted as part of Phase 6 acceptance
- No v1 requirements were assigned to Phase 5

The document should verify the phase goal: "All major surfaces polished to a cohesive, high-quality finish — fluid typography, focused timer overlay, immersive journal editor, intuitive day-strip history, and dual streak tracking in the header."

Key artifacts to verify exist: AppShell max-w container, TimerFocusOverlay, JournalEditorOverlay, DayStrip, SideNav, dual-streak Header, StreakCelebration.

**Commit:** `docs(07-01): backfill Phase 5 VERIFICATION.md`

---

### Task 6: Create 05-05-SUMMARY.md

**File:** `.planning/phases/05-ux-polish/05-05-SUMMARY.md`

Create a SUMMARY.md for Plan 05-05 (DayStrip replaces Heatmap, greeting header, dual-streak Header + visual acceptance).

Read `05-05-PLAN.md` to understand what was built, then read the actual files it modified to confirm what was implemented:
- `src/components/history/DayStrip.jsx`
- `src/pages/HistoryPage.jsx`
- `src/components/layout/Header.jsx`

The plan's visual acceptance checkpoint was approved during sessions #S55/#S56 (commits 9aed216, 6c5ff8d, b86bcb3 from 2026-03-10).

**Commit:** `docs(07-01): backfill 05-05-SUMMARY.md`

---

### Task 7: Update ROADMAP.md plan count

**File:** `.planning/ROADMAP.md`

Update Phase 7's plan count to `1/1` to reflect that 07-01 was created and executed.

Also update Phase 5's plan entry to reflect 05-05 is now documented.

**Commit:** (include in STATE.md update commit below)

---

### Final: Update STATE.md and ROADMAP.md

After all tasks complete, update STATE.md to reflect current position (Phase 7, Plan 01 in progress) and update ROADMAP.md plan progress via:

```bash
node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" roadmap update-plan-progress "07" "07-01" "complete"
```

Create SUMMARY.md for this plan.

**Commit:** `docs(07-01): complete cleanup and contract fixes plan`
