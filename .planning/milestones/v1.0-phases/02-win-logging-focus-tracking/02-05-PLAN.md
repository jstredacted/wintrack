---
phase: 02-win-logging-focus-tracking
plan: "05"
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - src/components/wins/TotalFocusTime.jsx
  - src/pages/TodayPage.jsx
autonomous: false
requirements: [WIN-01, WIN-02, WIN-03, WIN-04, TIMER-01, TIMER-02, TIMER-03]

must_haves:
  truths:
    - "User can see today's wins list on the Today page"
    - "User can open the full-screen win input overlay from Today page"
    - "User can log a win and see it appear immediately in the list"
    - "Roll-forward prompt appears automatically if yesterday had wins (once per session)"
    - "Total focused time is displayed and includes the live running timer contribution"
    - "Timer start/pause/stop works end-to-end from win card through to Supabase"
    - "All 7 requirements have automated tests passing"
  artifacts:
    - path: "src/components/wins/TotalFocusTime.jsx"
      provides: "Summed focus time display across all today's wins, null when 0"
      exports: ["default TotalFocusTime"]
    - path: "src/pages/TodayPage.jsx"
      provides: "Composed today view: RollForwardPrompt + WinList + WinInputOverlay + TotalFocusTime + add button"
      exports: ["default TodayPage"]
  key_links:
    - from: "src/pages/TodayPage.jsx"
      to: "src/hooks/useWins.js"
      via: "import { useWins }"
      pattern: "import.*useWins"
    - from: "src/pages/TodayPage.jsx"
      to: "src/stores/uiStore.js"
      via: "import { useUIStore }"
      pattern: "import.*useUIStore"
    - from: "src/pages/TodayPage.jsx"
      to: "src/components/wins/WinInputOverlay.jsx"
      via: "import WinInputOverlay"
      pattern: "import.*WinInputOverlay"
    - from: "src/components/wins/TotalFocusTime.jsx"
      to: "src/hooks/useStopwatch.js"
      via: "import { formatElapsed }"
      pattern: "import.*formatElapsed"
---

<objective>
Wire all components into TodayPage and add TotalFocusTime. Complete the phase with a visual checkpoint.

Purpose: This is the integration plan — all the independently-built pieces come together. TodayPage becomes the living app. After this plan, all 7 requirements are satisfied and verified.
Output: Fully functional Today view. TotalFocusTime tests green. Visual checkpoint confirms animated transitions work.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-win-logging-focus-tracking/02-RESEARCH.md
@.planning/phases/02-win-logging-focus-tracking/02-03-SUMMARY.md
@.planning/phases/02-win-logging-focus-tracking/02-04-SUMMARY.md
@src/components/wins/TotalFocusTime.test.jsx
@src/pages/TodayPage.jsx

<interfaces>
<!-- From 02-02: useWins return shape -->
const {
  wins,          // Win[] — today's wins ordered by created_at
  loading,       // boolean
  error,         // string | null
  yesterdayWins, // Win[] — yesterday's wins (for roll-forward prompt)
  addWin,        // (title: string) => Promise<void>
  editWin,       // (id: string, newTitle: string) => Promise<void>
  deleteWin,     // (id: string) => Promise<void>
  rollForward,   // () => Promise<void>
  startTimer,    // (id: string) => Promise<void>
  pauseTimer,    // (id: string, displaySeconds: number) => Promise<void>
  stopTimer,     // (id: string, displaySeconds: number) => Promise<void>
} = useWins();

<!-- From 02-02: uiStore return shape -->
const {
  inputOverlayOpen,
  editingWinId,
  rollForwardOffered,
  openInputOverlay,
  closeInputOverlay,
  markRollForwardOffered,
} = useUIStore();

<!-- Win object shape (from Supabase schema) -->
{
  id: string,
  title: string,
  win_date: string,     // 'YYYY-MM-DD'
  status: string,       // 'pending' by default
  timer_elapsed_seconds: number,
  timer_started_at: string | null,  // ISO timestamp or null
  created_at: string,
}

<!-- Anti-pattern to avoid: TIMER-03 total -->
// WRONG: sum only timer_elapsed_seconds from DB (misses live running timer contribution)
// RIGHT: include live delta for any win where timer_started_at is not null
// Total = wins.reduce((sum, w) => {
//   const live = w.timer_started_at
//     ? Math.floor((Date.now() - new Date(w.timer_started_at).getTime()) / 1000) : 0;
//   return sum + w.timer_elapsed_seconds + live;
// }, 0);
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement TotalFocusTime component</name>
  <files>src/components/wins/TotalFocusTime.jsx</files>
  <behavior>
    - Returns null (renders nothing) when total seconds across all wins is 0
    - Displays formatted total when wins have accumulated time
    - Includes live delta from timer_started_at for any win where timer is running
    - Re-renders every second when any timer is running (setInterval for tick only, not accumulation)
  </behavior>
  <action>
    Create `src/components/wins/TotalFocusTime.jsx`.

    Props: `{ wins: Win[] }`

    Use the pattern from RESEARCH.md exactly — track a `tick` state with a 1-second interval conditional on `anyRunning`. Compute the total using wall-clock math including live deltas.

    Display format: `"Focus today: MM:SS"` or `"Focus today: HH:MM:SS"` using `formatElapsed` from `@/hooks/useStopwatch`.

    Style: `text-xs font-mono uppercase tracking-[0.2em] text-muted-foreground`

    After creating the file, run: `npx vitest run src/components/wins/TotalFocusTime.test.jsx -x`
    All TotalFocusTime tests must pass before proceeding.
  </action>
  <verify>
    <automated>npx vitest run src/components/wins/TotalFocusTime.test.jsx --reporter=verbose</automated>
  </verify>
  <done>All TotalFocusTime tests green. Returns null at 0, shows sum including live timer delta when running.</done>
</task>

<task type="auto">
  <name>Task 2: Wire TodayPage — compose all components</name>
  <files>src/pages/TodayPage.jsx</files>
  <action>
    Replace the placeholder TodayPage with the fully composed view.

    TodayPage owns the data layer (calls useWins and useUIStore directly).

    Layout from top to bottom within the page scroll area:
    1. Date header — `getLocalDateString()` in muted text (keep from original)
    2. RollForwardPrompt — shown only if: `yesterdayWins.length > 0 AND !rollForwardOffered`. On confirm: call `rollForward()` then `markRollForwardOffered()`. On dismiss: call `markRollForwardOffered()` only.
    3. TotalFocusTime — receives `wins` prop; renders nothing when totalSeconds=0
    4. WinList — receives wins, onEdit, onDelete, onStartTimer, onPauseTimer, onStopTimer wired to useWins actions
    5. WinInputOverlay — receives `open={inputOverlayOpen}`, `onSubmit={async (title) => { await addWin(title); closeInputOverlay(); }}`, `onClose={closeInputOverlay}`
    6. "Log a win" / "+" button — fixed bottom-right or below the list, calls `openInputOverlay()`. Use a ghost/outline button with Lucide `Plus` icon. Do NOT use a bottom nav position (conflicts with BottomTabBar).

    Handle loading state: show a subtle loading indicator or empty state while `loading=true`.
    Handle error state: show a small error message if `error` is set (muted text, font-mono).

    WinList callback wiring:
    ```jsx
    <WinList
      wins={wins}
      onEdit={(id, newTitle) => editWin(id, newTitle)}
      onDelete={(id) => deleteWin(id)}
      onStartTimer={(id) => startTimer(id)}
      onPauseTimer={(id, secs) => pauseTimer(id, secs)}
      onStopTimer={(id, secs) => stopTimer(id, secs)}
    />
    ```
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>Full test suite green (all 5 test files passing). TodayPage composes all Phase 2 components with correct data wiring. npm run build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual acceptance checkpoint — Phase 2</name>
  <what-built>
    Full Phase 2 implementation: win input overlay with AnimatePresence transitions, win list with inline edit/delete, per-win stopwatch, roll-forward prompt, and total focus time display.
  </what-built>
  <how-to-verify>
    Start the dev server: npm run dev, then visit http://localhost:5173

    1. Win input flow (WIN-01):
       - Click the "Log a win" / "+" button
       - Verify: full-screen overlay slides in from below
       - Type a win title (e.g., "Shipped the win input overlay") and press Enter
       - Verify: overlay slides out, win appears in the list immediately

    2. Edit and delete (WIN-02, WIN-03):
       - Click the Pencil icon on the win just created
       - Verify: title becomes an editable input pre-filled with the title
       - Change the title and press Enter — verify it updates
       - Click Pencil again, press Escape — verify it cancels
       - Click the Trash2 icon — verify the win is removed from the list

    3. Roll-forward (WIN-04):
       - This requires having wins from yesterday in Supabase. If no yesterday wins exist, skip this step and note it.
       - If yesterday wins exist: verify a prompt appears at the top of the page, confirm it, verify wins are added

    4. Stopwatch (TIMER-01, TIMER-02):
       - Log another win
       - Click Play on the win card — verify elapsed time starts counting up (MM:SS format)
       - Switch to another browser tab, wait 10 seconds, switch back — verify elapsed time is correct (not reset)
       - Click Pause — verify time freezes at the correct value
       - Reload the page — verify the elapsed time is still shown correctly (wall-clock recovery from Supabase)
       - Click Play again, then Square (stop) — verify time is saved

    5. Total focus time (TIMER-03):
       - With at least one win that has elapsed time: verify "Focus today: MM:SS" appears
       - Start a timer — verify the total focus time updates live each second

    6. Animation (visual, WIN-01):
       - Open and close the overlay 2-3 times — verify smooth slide-in/slide-out animation
  </how-to-verify>
  <action>Run npm run dev. Navigate to http://localhost:5173. Follow the verification steps above for all 6 areas. Note any failures.</action>
  <verify>
    <automated>npx vitest run --reporter=verbose</automated>
  </verify>
  <done>User types "approved" confirming all Phase 2 flows work correctly in the browser.</done>
  <resume-signal>Type "approved" if all flows work, or describe specific issues found</resume-signal>
</task>

</tasks>

<verification>
npx vitest run — all 5 test suites green (useStopwatch, WinInputOverlay, WinCard, RollForwardPrompt, TotalFocusTime).
npm run build — clean build, no errors.
Visual checkpoint: all Phase 2 flows verified in browser.
</verification>

<success_criteria>
- TotalFocusTime tests green (TIMER-03 covered)
- Full test suite green: all 5 Phase 2 test files passing
- TodayPage composes all components with correct data wiring
- Visual checkpoint: animated overlay, live stopwatch, page-refresh recovery, total focus time all confirmed working
- All 7 requirements (WIN-01 through WIN-04, TIMER-01 through TIMER-03) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-win-logging-focus-tracking/02-05-SUMMARY.md`
</output>
