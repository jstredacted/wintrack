---
phase: 02-win-logging-focus-tracking
plan: "04"
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/components/wins/WinCard.jsx
  - src/components/wins/RollForwardPrompt.jsx
autonomous: true
requirements: [WIN-02, WIN-03, WIN-04, TIMER-01, TIMER-02]

must_haves:
  truths:
    - "User can click edit on a win card and type a new title, confirmed with Enter"
    - "User can press Escape to cancel an inline edit"
    - "User can click delete to remove a win"
    - "Win card shows a stopwatch with start/pause/stop buttons"
    - "Elapsed time on the win card is correct and updates live when the timer is running"
    - "Roll-forward prompt shows the correct count and fires correct callbacks"
  artifacts:
    - path: "src/components/wins/WinCard.jsx"
      provides: "Win card with inline edit, delete, and per-win stopwatch display"
      exports: ["default WinCard"]
    - path: "src/components/wins/RollForwardPrompt.jsx"
      provides: "Inline banner showing yesterday's incomplete wins with confirm/dismiss"
      exports: ["default RollForwardPrompt"]
  key_links:
    - from: "src/components/wins/WinCard.jsx"
      to: "src/hooks/useStopwatch.js"
      via: "import { useStopwatch, formatElapsed }"
      pattern: "import.*useStopwatch"
    - from: "src/components/wins/WinCard.jsx"
      to: "lucide-react"
      via: "import { Pencil, Trash2, Play, Pause, Square } from 'lucide-react'"
      pattern: "from.*lucide-react"
---

<objective>
Build WinCard (WIN-02, WIN-03, TIMER-01, TIMER-02) and RollForwardPrompt (WIN-04).

Purpose: This plan runs in parallel with 02-03 — both depend only on 02-02. No shared files. Together with 02-03, these two plans complete all components. 02-05 then wires everything into TodayPage.
Output: WinCard and RollForwardPrompt components. WinCard, RollForwardPrompt tests turn green.
</objective>

<execution_context>
@/Users/justin/.claude/get-shit-done/workflows/execute-plan.md
@/Users/justin/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-win-logging-focus-tracking/02-RESEARCH.md
@.planning/phases/02-win-logging-focus-tracking/02-02-SUMMARY.md
@src/components/wins/WinCard.test.jsx
@src/components/wins/RollForwardPrompt.test.jsx

<interfaces>
<!-- From 02-02: useStopwatch -->
import { useStopwatch, formatElapsed } from '@/hooks/useStopwatch';
// useStopwatch(elapsedBase: number, startedAt: string | null)
// returns: { displaySeconds: number, isRunning: boolean }
// formatElapsed(seconds: number) → 'MM:SS' or 'HH:MM:SS'

<!-- Lucide icons (already installed) -->
import { Pencil, Trash2, Play, Pause, Square } from 'lucide-react';

<!-- Design tokens -->
// font-mono, text-foreground, text-muted-foreground, border border-border
// text-xs, text-sm, rounded, p-4

<!-- Stopwatch button states -->
// idle/paused:    [Play]  [no pause]  [no stop]
// running:        [Pause] [Stop]
// Stop = save elapsed and return to idle (same behavior as pause for Phase 2)

<!-- WinCard props interface -->
// { win: { id, title, timer_elapsed_seconds, timer_started_at }, onEdit, onDelete, onStartTimer, onPauseTimer, onStopTimer }

<!-- Inline edit UX (Claude's discretion — inline, not modal) -->
// Clicking Pencil icon enters edit mode: title text replaced with <input> pre-filled
// Enter submits → calls onEdit(win.id, newTitle)
// Escape cancels → restores original title display
// Empty submit ignored (no call to onEdit)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement WinCard with inline edit and stopwatch</name>
  <files>src/components/wins/WinCard.jsx</files>
  <behavior>
    - Renders win.title as display text when not in edit mode
    - Clicking the Pencil icon enters edit mode (input appears, pre-filled with current title)
    - Pressing Enter in edit mode with non-empty title calls onEdit(win.id, newTitle.trim()), exits edit mode
    - Pressing Escape in edit mode cancels without calling onEdit, restores display
    - Clicking Trash2 icon calls onDelete(win.id)
    - When not running (timer_started_at=null): shows Play button only
    - When running (timer_started_at set): shows Pause and Square (stop) buttons
    - Clicking Play calls onStartTimer(win.id)
    - Clicking Pause calls onPauseTimer(win.id, displaySeconds)
    - Clicking Square (stop) calls onStopTimer(win.id, displaySeconds)
    - Displays formatted elapsed time (formatElapsed(displaySeconds)) from useStopwatch
  </behavior>
  <action>
    Create `src/components/wins/WinCard.jsx`.

    Props: `{ win, onEdit, onDelete, onStartTimer, onPauseTimer, onStopTimer }`

    Use local `useState` for `isEditing` and `editValue` — these are UI-only states, not in uiStore.

    Use `useStopwatch(win.timer_elapsed_seconds, win.timer_started_at)` to get `{ displaySeconds, isRunning }`.

    Card layout (Claude's discretion — compact, monospaced, Nothing Phone aesthetic):
    - Full border with font-mono text
    - Title row: title text (display mode) OR input (edit mode) + Pencil icon
    - Action row: elapsed time display + stopwatch buttons + Trash2

    After creating the file, run: `npx vitest run src/components/wins/WinCard.test.jsx -x`
    All WinCard tests must pass before proceeding.
  </action>
  <verify>
    <automated>npx vitest run src/components/wins/WinCard.test.jsx --reporter=verbose</automated>
  </verify>
  <done>All WinCard tests green. Inline edit, delete, stopwatch buttons, and elapsed display all verified by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement RollForwardPrompt</name>
  <files>src/components/wins/RollForwardPrompt.jsx</files>
  <behavior>
    - Renders "1 unfinished win from yesterday — carry forward?" for count=1
    - Renders "3 unfinished wins from yesterday — carry forward?" for count=3 (plural)
    - Clicking "Yes" calls onConfirm
    - Clicking "Dismiss" calls onDismiss
  </behavior>
  <action>
    Create `src/components/wins/RollForwardPrompt.jsx`.

    Props: `{ count: number, onConfirm: () => void, onDismiss: () => void }`

    Use the exact structure from RESEARCH.md:
    ```jsx
    export default function RollForwardPrompt({ count, onConfirm, onDismiss }) {
      return (
        <div className="border border-border rounded p-4 flex items-center justify-between gap-4 text-sm font-mono">
          <span className="text-muted-foreground">
            {count} unfinished win{count !== 1 ? 's' : ''} from yesterday — carry forward?
          </span>
          <div className="flex gap-2">
            <button onClick={onConfirm} className="text-foreground underline">Yes</button>
            <button onClick={onDismiss} className="text-muted-foreground">Dismiss</button>
          </div>
        </div>
      );
    }
    ```

    After creating the file, run: `npx vitest run src/components/wins/RollForwardPrompt.test.jsx -x`
    All RollForwardPrompt tests must pass before proceeding.
  </action>
  <verify>
    <automated>npx vitest run src/components/wins/RollForwardPrompt.test.jsx --reporter=verbose</automated>
  </verify>
  <done>All RollForwardPrompt tests green. Singular/plural count, onConfirm, onDismiss all verified.</done>
</task>

</tasks>

<verification>
npx vitest run src/components/wins/WinCard.test.jsx src/components/wins/RollForwardPrompt.test.jsx — all green.
npx vitest run src/hooks/ — useStopwatch tests still green, no regressions.
</verification>

<success_criteria>
- WinCard: inline edit (Enter/Escape), delete, stopwatch (start/pause/stop), elapsed display all tested
- RollForwardPrompt: count display (singular/plural), onConfirm, onDismiss all tested
- No regressions in existing test suite
</success_criteria>

<output>
After completion, create `.planning/phases/02-win-logging-focus-tracking/02-04-SUMMARY.md`
</output>
